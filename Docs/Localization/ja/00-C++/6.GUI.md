---
comments: true
---

# GUI

> グラフィカルユーザーインターフェース（Graphical User Interface、略してGUI、グラフィカルユーザーインターフェースとも呼ばれる）は、グラフィック方式で表示されるコンピュータ操作のユーザーインターフェースを指す。
>
> グラフィカルユーザーインターフェースは人と[コンピュータ通信](https://baike.baidu.com/item/计算机通信/8082711?fromModule=lemma_inlink)を行うためのインターフェース表示[形式](https://baike.baidu.com/item/格式/2406?fromModule=lemma_inlink)であり、ユーザーが[マウス](https://baike.baidu.com/item/鼠标/122323?fromModule=lemma_inlink)などの入力デバイスを使って[画面](https://baike.baidu.com/item/屏幕/3750314?fromModule=lemma_inlink)上の[アイコン](https://baike.baidu.com/item/图标/3823326?fromModule=lemma_inlink)やメニューオプションを操作し、コマンドの選択、ファイルの呼び出し、プログラムの起動、その他の[日常的なタスク](https://baike.baidu.com/item/日常任务/10744783?fromModule=lemma_inlink)を実行できるようにする。キーボードからテキストや文字コマンドを入力して日常的なタスクを完了する文字インターフェースと比較して、グラフィカルユーザーインターフェースには多くの利点がある。グラフィカルユーザーインターフェースはウィンドウ、ドロップダウンメニュー、ダイアログボックスおよびそれに対応する制御メカニズムで構成されており、様々な新しいアプリケーションで標準化されている。つまり、同じ操作は常に同じ方法で完了し、グラフィカルユーザーインターフェースではユーザーが見て操作するのはすべてグラフィックオブジェクトであり、コンピュータグラフィックス技術が応用されている。
>
> ——《[百度百科](https://baike.baidu.com/item/GUI/479966)》

コンソールと知恵比べをする日々に疲れたら、この記事が役に立つかもしれません。

## 概要

GUIの分類は主に2つの大きなカテゴリに分かれている。

- **Native UI**：伝統的なネイティブアプリ開発モデルで、システムプラットフォーム上のインターフェースを使用して開発する
- **Web UI**：ブラウザ上で特定的に動作するウェブアプリケーション

こちらに更に詳しい紹介があります：

- [知乎：Native、Web App、Hybridとは何か？](https://zhuanlan.zhihu.com/p/40705635)

> HybridとはNative UIとWeb UIを混合して開発することを指す

それらに関する議論はこちらを参照してください：

- [知乎：Web AppとNative App、どちらがトレンドか？](https://www.zhihu.com/question/19558750)

Web UIはコストが低く開発が速いため、インターネット市場で主導的地位を占めていますが、Native UIを使用する理由は主に高性能、カスタマイズ性、ハードウェアとのインタラクションです。

[Web GPU](https://www.zhihu.com/question/315103318)が現在安定しつつあり、Webプログラマーにとっては更なる向上の余地がありますが、グラフィックスやエンジン分野の方には、Native方向を進むことを推奨します。これにより、より多くの制御可能な空間が得られます。

Native UIの方案は、言語によって主に以下のように分類されます：

C/C++: [Qt](https://www.qt.io/product/framework)、[WxWidget](https://www.wxwidgets.org/)、[GTK](https://www.gtk.org/)、[FLTK](https://www.fltk.org/)、[DearImGUI](https://github.com/ocornut/imgui/wiki)、[Nuklear](https://github.com/vurtun/nuklear)、[duilib](https://github.com/duilib/duilib)、[C++ Builder](https://www.embarcadero.com/cn/products)、[MFC](https://learn.microsoft.com/en-us/cpp/mfc/mfc-desktop-applications)、[EasyX](https://easyx.cn/)

Python: [PyQt](https://riverbankcomputing.com/software/pyqt/)、[WxPython](https://www.wxpython.org/)、[PyGTK](https://www.gtk.org/)、[PyFLTK](https://pyfltk.sourceforge.io/)、[DearPyGUI](https://github.com/hoffstadt/DearPyGui/wiki)、[Tkinter](https://docs.python.org/3/library/tkinter.html)、[PyGame](https://www.pygame.org/news)

C#：[WPF](https://learn.microsoft.com/en-us/dotnet/desktop/wpf/overview/)、[WinForm](https://learn.microsoft.com/en-us/dotnet/desktop/winforms/overview)

Java：Swing、[JavaFx](https://openjfx.io/)

上記のフレームワークの中で、C/C++を使用すると天然の優位性があります。もしPythonやC#の開発者で、強制的な業務要件がない場合は、GUIのために大掛かりなC++の学習をする必要はありません。

- [知乎：どの言語がGUI作成に最も適しているか？](https://www.zhihu.com/question/276815517)

これらのC++ GUIフレームワークの中で、どのように選択すれば良いのでしょうか？

筆者は順番にEasyX - Tkinter - PyGame - JavaFx - FLTK - Qt - ImGUI - UE Slateを学びました。

全体的に使ってみると、GUIは結局以下のことであることが分かりました：

- システムの（キーボード・マウス）インタラクションイベントを処理する
- 画面上にピクセルを埋めていくつかのグラフィックスを描画する

これはオペレーティングシステムまたは各GUIフレームワークが少なくとも提供する機能であり、この2つの機能があれば、キーボード・マウス操作にコールバック関数をバインドし、ループで画面ピクセルにRGBを埋めるだけで、どんなクールなグラフィカルインターフェースでも作れませんか？例えば以下のようなプログラムです。

``` c++
#include <conio.h>		//コンソールIO
#include <iostream>

int main() {
	char currentChar = '\0';
	const int width = 20;
	const int height = 20;
	while (true) {
		if (kbhit()) {				//kbhitは現在キー入力があるかどうかを判断できる
			currentChar = getch();	//getchはエコーなし入力関数で、getcharと異なり、入力したキーがコンソールに表示されない
		}
		system("cls");				//コンソールをクリアする
		std::cout << "Please enter key:" << std::endl;
		for (int y = 0; y < height; y++) {
			for (int x = 0; x < width; x++) {
				std::cout << currentChar;
			}
			std::cout << std::endl;
		}
	}
    return 0;
}
```

![image-20230211103821026](../../../00-C++/Resources/image-20230211103821026.png)

> コンソールプログラムは実行中に激しく点滅します。この点については後述で解決します。

GUIは上記のコンソールプログラムと比較して、結局キーボード・マウス入力を一層のインターフェースにカプセル化し、`width`と`height`をウィンドウサイズとし、対応する座標のピクセルに色を埋めるだけです。

実際にどのフレームワークを選択するかを決定する最も重要な点は生態系であり、具体的には以下の点に現れます：

- 使用する人が多く、チームメンバーが多く、採用が容易である
- フレームワークのカバレッジが広く、ツールライブラリが多く、ドキュメントが充実している
- 公式が長期的に維持し、迭代頻度が高く、不具合が少ない

上記のフレームワークの中で、最も良くできているのは間違いなくQtです。

Qtの大部分のモジュールはLGPLライセンスに従っており、動的リンクの形式でモジュールを使用し、商用ソフトウェアをリリースすることが許可されています。各モジュールのオープンソースライセンスについては以下を参照してください：

- https://doc.qt.io/qt-6/licenses-used-in-qt.html

Qtのソースコードを変更したり、一部の商用モジュールを使用したりする場合は、Qtの商用ライセンスを購入する必要があります。具体的には以下を参照してください：

- https://www.qt.io/licensing/open-source-lgpl-obligations

Qtのオープンソースライセンスが比較的厳しく、C++標準とは独立しており、体量が膨大であるため、多くのチームは依然としてWxWidget、C++ Builder、duilib、GTKなどのフレームワークを選択する可能性があります。MFC、FLTK、EasyXは良いGUIフレームワークかもしれませんが、開発生態系は楽観的ではないため、選択肢がある場合は触れないことを推奨します。

Dear ImGUIとNuklearは軽量、ポータブルで**Immediate Mode**に従うGUIツールキットであり、本質的には開発者にいくつかのコントロールインターフェースを提供して一フレームのグラフィックスの描画を完了させるものです。ImGUIを例にとると、以下のコードを使用します：

``` c++
ImGui::Text("Hello Dear ImGUI");
static bool CheckState;
ImGui::Checkbox("CheckBox ", &CheckState);
```

以下のようなインターフェースを生成できます：

![image-20230210205746160](../../../00-C++/Resources/image-20230210205746160.png)

**Immediate Mode（即時モード）** は主に変数`CheckState`のポインタを「コントロール」に渡す点に現れます。一方、伝統的な**Retained Mode（保持モード）** 、例えばQtでは、コードは以下のようになる可能性があります：

``` c++
static bool CheckState;
QCheckBox* CheckBox = new QCheckBox;
CheckBox->setCheckState( CheckState? Qt::Checked : Qt::Unchecked);		//初期化
QObject::connect(CheckBox,&QCheckBox::stateChanged,[&](int state){		//signal-slotをバインド
    CheckState = state == Qt::Checked;
});
```

**QCheckBox** が`CheckState`のポインタを使用していないことがわかります。代わりに`CheckState`を使用してコントロールの初期状態を設定し、コントロールの`stateChanged`時に`CheckState`の値を設定します。つまり、コントロール自体が自身のStateを保持しているのに対し、**Immediate Modeは無状態を強調**し、状態をコントロールに埋め込んでいます。

開発過程で、过多の局部状態を保存すると同期を保持するために多くのコードを記述する必要があり、不注意でバグを引き起こす可能性があります。例えば、一部のイベントが複数回実行されたり、無限ループに陥ったりしやすくなります。

例えば、Colorを調整する場合：

![](../../../00-C++/Resources/image-20230210212343936.png)

カラーパレットを使用し、ドラッグまたはチャネル値の入力、16進数入力、画面からの色取得など、複数の方式で同じ色値を調整できます。保持モードを使用する場合、ある方式で色を変更した後、他のコントロールを手動で調整して色の同期を保証する必要があります。いくつかのデザインパターンに従うことでこの問題を解決できますが、即時モードはこのようなアプリケーションシナリオで天然の優位性を持っています。それらの違いに関する議論は以下を参照してください：

- [知乎：RetainedとIMGUIの関係と違いは何か？](https://www.zhihu.com/question/39093254/answer/1351958747)

では、これはDear ImGUIがQtより優れていることを意味するのでしょうか？必ずしもそうではありません。筆者の観点は以下の通りです：

- **Immediate Modeはゲームエンジンにより適しているが、Dear ImGUIはまだ不十分である。** それは単なる小さなGUIツールキットであり、確かに使いやすく、良いインターフェース効果を作り出すことができますが、大規模なソフトウェアシステムの開発はエンジニアリングアートであり、低い門檻はより多くの問題を引き起こす可能性があります。十分な人手と精力で不断に迭代できない限り、ネイティブのDear ImGUIを使用して開発することは災難になります...

  > ここに回答があります。Dear ImGUIについて言及していませんが、この種のフレームワークの問題をよく暴露しています：https://www.zhihu.com/question/66934513/answer/248036488

- **Retained Modeはコントロールアーキテクチャの一種の方式に過ぎず、Qt != Retained Modeである。** 基本的にネイティブUIフレームワークの公式コントロールはRetained Modeを使用していますが、希望する場合はこれらのフレームワーク上でImmediate Modeを使用することもできます。

  > [Why Qt and not IMGUI?](https://deplinenoise.wordpress.com/2017/03/05/why-qt-and-not-imgui/)

では、Qtの方が良いという意味でしょうか？必ずしもそうではありません。筆者の観点は以下の通りです：

- Qtはエディタとして主に使用されるため、ゲームエンジン内では多くの場合、以下の例のようになります：
  - [O3DE](https://www.o3de.org/)：https://github.com/o3de/o3de

現在、ゲーム内のUI方案には適していません：

- 云风のBLOG — ゲームUIモジュールの選択：https://blog.codingnow.com/2020/07/game_ui.html

UEのSlateは非常に優れた基盤UIフレームワークです：

- https://docs.unrealengine.com/5.1/en-US/understanding-the-slate-ui-architecture-in-unreal-engine/

しかし、本シリーズ記事の次のグラフィックス関連のチュートリアルでは、Qtを主エディタとして使用し、ImGUIをシーン編集のために埋め込む予定です。主な理由は以下の通りです：

- QtはクロスプラットフォームのWindowカプセル化を持ち、Handleを取得してNative APIを使用して特殊な処理を行うことができる
- Qtはエレガントなリフレクションシステムを持ち、エディタの構築に便利であり、Signal-Slotを使用してロジックを良好にデカップリングし、可読性を高めることができる
- Qtは独立したコンテナライブラリを持ち、STLの苦しみから免れることができる
- Qtはインターフェーススタイルが統一された多くの拡張ライブラリを持ち、例えば基礎数学ライブラリ、画像エンコーディング・デコーディングライブラリ、マルチメディアライブラリなどがあり、学習成果をより容易に実用化することができる
- Qt 6.0以降、OpenGL、DX11、Vulkan、MetalをサポートするRHI（Render Hardware Interface）が導入され、OpenGLより使用が容易であり、アーキテクチャ上はVulkanスタイルのglslと現代的なグラフィックスAPIを使用し、bgfxのような魔改造を行っていません。最も重要なのは、QtのRHIを使用してNative APIと良好に互換性を持たせることができ、これによりグラフィックスAPIまたはハードウェアが提供する高級機能を使用しやすくなります
- Qtの多くの体系構造はUnreal Engineにマッピングすることができ、目標がUnreal Engineである場合は、Qtは最良の跳板であり、Qtの公式コードの可読性が高く、大量のドキュメント、Demo、チュートリアルがあるためです

## プラットフォーム基礎

GUIプログラムは画面に表示され、画面上のピクセルをシステム的に管理するために、オペレーティングシステムはWindowという構造を提供します。Windows10では、標準的なWindowは以下のようになり、タイトルバー（アイコン、タイトルテキスト、最小化ボタン、最大化ボタン、閉じるボタン）を持っています：

![](../../../00-C++/Resources/image-20230210223818978.png)

以下の方法でWindowを作成することができます：

- `CMakeLists.txt`と`main.cpp`ファイルを新規作成する

- `CMakeLists.txt`に以下を記入する：

  ``` cmake
  cmake_minimum_required(VERSION 3.12)
  project(Window)
  add_executable(Window WIN32 main.cpp)
  ```

  > Win32はこのexecutableがWin32ウィンドウであることを示し、具体的にはVisual Studioの`プロジェクト設定` - `リンカ` - `システム` - `サブシステム` - `ウィンドウ`に対応する
  >
  > ![image-20230210225528304](../../../00-C++/Resources/image-20230210225528304.png)

- `main.cpp`に以下を記入する：

  ``` c++
  #ifndef UNICODE
  #define UNICODE
  #endif 
  
  #include <windows.h>
  
  LRESULT CALLBACK WindowProc(HWND hwnd, UINT uMsg, WPARAM wParam, LPARAM lParam);
  
  int WINAPI wWinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, PWSTR pCmdLine, int nCmdShow)
  {
      // ウィンドウクラスを登録する
      const wchar_t CLASS_NAME[]  = L"Sample Window Class";
      
      WNDCLASS wc = { };
  
      wc.lpfnWndProc   = WindowProc;
      wc.hInstance     = hInstance;
      wc.lpszClassName = CLASS_NAME;
  
      RegisterClass(&wc);
  
      // ウィンドウを作成する
  
      HWND hwnd = CreateWindowEx(
          0,                              // オプションのウィンドウスタイル
          CLASS_NAME,                     // ウィンドウクラス
          L"Learn to Program Windows",    // ウィンドウテキスト
          WS_OVERLAPPEDWINDOW,            // ウィンドウスタイル
  
          // サイズと位置
          CW_USEDEFAULT, CW_USEDEFAULT, CW_USEDEFAULT, CW_USEDEFAULT,
  
          NULL,       // 親ウィンドウ    
          NULL,       // メニュー
          hInstance,  // インスタンスハンドル
          NULL        // 追加のアプリケーションデータ
          );
  
      if (hwnd == NULL)
      {
          return 0;
      }
  
      ShowWindow(hwnd, nCmdShow);
  
      // メッセージループを実行する
  
      MSG msg = { };
      while (GetMessage(&msg, NULL, 0, 0) > 0)
      {
          TranslateMessage(&msg);
          DispatchMessage(&msg);
      }
  
      return 0;
  }
  
  LRESULT CALLBACK WindowProc(HWND hwnd, UINT uMsg, WPARAM wParam, LPARAM lParam)
  {
      switch (uMsg)
      {
      case WM_DESTROY:
          PostQuitMessage(0);
          return 0;
  
      case WM_PAINT:
          {
              PAINTSTRUCT ps;
              HDC hdc = BeginPaint(hwnd, &ps);
  
              // すべての描画はここで行われ、BeginPaintとEndPaintの間である
  
              FillRect(hdc, &ps.rcPaint, (HBRUSH) (COLOR_WINDOW+1));
  
              EndPaint(hwnd, &ps);
          }
          return 0;
  
      }
      return DefWindowProc(hwnd, uMsg, wParam, lParam);
  }
  ```

- CMakeコマンドラインまたはCMakeGUIを使用してVisualStudioプロジェクトファイルを生成し、`ソリューションエクスプローラー`でWindowプロジェクトを右クリックして`スタートアッププロジェクトに設定`し、実行すると以下が表示されます：

  ![image-20230210225702843](../../../00-C++/Resources/image-20230210225702843.png)

上記のコードはマイクロソフトの公式チュートリアルからのもので、興味のある読者は自分で一通り見ることができます：

- https://learn.microsoft.com/en-us/windows/win32/learnwin32/your-first-windows-program

### Spy++

ここまでで、Windowとは何だと思いますか？最大化、最小化、閉じるボタンの付いた枠ですか？

必ずしもそうではありません。Visual Studioで**Spy++**を見つけることができます：`メニューバー`—`ツール`—`Spy++`

![image-20230210231111020](../../../00-C++/Resources/image-20230210231111020.png)

これを使用してシステム内のすべてのWindowを表示することができます：

![image-20220523194419184](../../../00-C++/Resources/image-20220523194419184.png)

> Spy++は自底向上で階層的にすべてのウィンドウを表示します。つまり、ディスプレイに表示されるものはすべて痕跡があり、例えば最下層はデスクトップ（壁紙）です。

そのウィンドウ検索ツール（`メニューバー`—`検索`—`ウィンドウを検索`—`ドラッグ`下図の照準を任意のウィンドウに）を使用して、Spy++で具体的な`Item`を定位することができます。これはUEのコントロールリフレクターに類似しています。

![image-20220523194840789](../../../00-C++/Resources/image-20220523194840789.png)

上記のツールを通じて、ウィンドウには階層があり、必ずしも閉じるボタンがあるわけではなく、枠さえない可能性があることがわかります。

![image-20220523195615080](../../../00-C++/Resources/image-20220523195615080.png)

Windowsでは、枠とタイトルバーの部分はシステムによって制御され、プログラム自体に属さないため、**Non-Client Area（非クライアント領域）**と呼ばれます。

枠内部の領域はプログラムが自由に描画できる領域であり、この部分は**Client Area（クライアント領域）**と呼ばれます。

- [What is windows client and non-client area?](https://www.equestionanswers.com/vcpp/client-non-client-area.php)

マイクロソフトはタイトルバーがUIの美化を制限していることに気づいた可能性があるため、ウィンドウは**枠なし且つ背景透明**の属性を設定することをサポートしています。多くのソフトウェアはこの方式で**異形ウィンドウ**を描画しています。例えばQQ音楽のプレーヤー：

![image-20230210223110058](../../../00-C++/Resources/image-20230210223110058.png)

システムインタラクションイベントは現在のActivated（アクティブな）ウィンドウに伝達され、Unactivated状態にあるウィンドウはシステムが一般的にグレーのスタイルで表示します：

![](../../../00-C++/Resources/image-20230210223752691.png)

> 一般的に、アクティブなウィンドウは1つだけであり、これは現在キーボードの入力イベントを受け入れるウィンドウが1つだけであることを意味します。ただし、[フック](https://baike.baidu.com/item/%E9%92%A9%E5%AD%90)などの方法で規則を打破することもできます。

### Multiple buffering

以前のコンソールプログラムでは、ページが激しく点滅します。これは1つのバッファに少しずつものを追加し、コンソールウィンドウがこの過程をプレビューしているため、リフレッシュ速度が速いため点滅が発生するからです。このような問題を解決するために、コンピュータ科学では**Multiple buffering（多バッファ）**の概念が提唱されました：

- コンソールプレビューに供給するバッファを**前台バッファ**と呼び、多バッファの核心は前台バッファを直接操作するのではなく、**后台バッファ**を処理した後、**一括して**前台バッファに**提交**することで、点滅と撕裂を効果的に回避できます。

> 一括提交といえば、前節の**for copy**と**memcpy**を思い出しませんか？
>
> かごの中のすべてのオレンジを別のかごに入れる場合、1つずつ入れる方が速いですか？それとも直接かごを持ち上げて倒す方が速いですか？明らかに後者です。
>
> ハードウェアとインターフェースの提供者も明らかにこの道理を理解しており、大多数の場合、Map、Blitのような操作をサポートしています。

コンソールで双バッファを使用する例は以下の通りです：

``` c++
#include <conio.h>		//コンソールIO
#include <iostream>
#include <Windows.h>

const int width = 20;
const int height = 20;
const int MaxBufferSize = width * height * 10;

void UseSingleBuffer() {
	char currentChar = '*';
	while (true) {
		if (kbhit()) {				//kbhitは現在キー入力があるかどうかを判断できる
			currentChar = getch();	//getchはエコーなし入力関数で、getcharと異なり、入力したキーがコンソールに表示されない
		}
		system("cls");				//コンソールをクリアする
		for (int y = 0; y < height; y++) {
			for (int x = 0; x < width; x++) {
				std::cout << currentChar;
			}
			std::cout << std::endl;
		}
	}
}

void UseDoubleBuffer() {
	HANDLE frontendBuffer = GetStdHandle(STD_OUTPUT_HANDLE);		//デフォルトのバッファを取得する

	HANDLE backendBuffer = CreateConsoleScreenBuffer(				//新しいバッファを作成して后台バッファとする
		GENERIC_READ | GENERIC_WRITE,
		FILE_SHARE_READ | FILE_SHARE_WRITE,
		NULL,
		CONSOLE_TEXTMODE_BUFFER,
		NULL
	);

	//2つのバッファのカーソルを隠す
	CONSOLE_CURSOR_INFO cci;
	cci.bVisible = 0;
	cci.dwSize = 1;
	SetConsoleCursorInfo(frontendBuffer, &cci);
	SetConsoleCursorInfo(backendBuffer, &cci);


	char bufferData[MaxBufferSize];		//キャッシュデータ一時保存領域
	DWORD bufferLength = 0;
	COORD zeroCoord = { 0,0 };
	char currentChar = '*';

	while (true) {
		if (kbhit()) {				
			currentChar = getch();	
		}
		bufferLength = 0;

		CONSOLE_SCREEN_BUFFER_INFO backendBufferInfo; 		//backendBufferの每行の文字数を取得する
		GetConsoleScreenBufferInfo(backendBuffer, &backendBufferInfo);
		int lineWidth = backendBufferInfo.srWindow.Right - backendBufferInfo.srWindow.Left + 1;		

		for (int y = 0; y < height; y++) {
			for (int x = 0; x < width; x++) {
				bufferData[bufferLength++] = currentChar;
			}
			while (bufferLength % lineWidth != 0) {				//改行の効果を達成するために文字を埋める
				bufferData[bufferLength++] = ' ';
			}
		}
        
        //backendBufferのzeroCoordから始めてbufferLength長のbufferDataを書き込む
		WriteConsoleOutputCharacterA(backendBuffer, bufferData, bufferLength, zeroCoord, &bufferLength);	
		SetConsoleActiveScreenBuffer(backendBuffer);			//backendBufferを表示する
		std::swap(backendBuffer, frontendBuffer);				//前后バッファ操作ハンドルを交換し、frontendBufferが次のループのbackendBufferとなる
	}
}

int main() {
	//UseSingleBuffer();
    UseDoubleBuffer();
	return 0;
}
```

大多数のGUIフレームワークは双バッファの方式を使用し、ゲームではさらに三バッファを使用する場合があります。一般的にこの構造を[交换链](https://en.wikipedia.org/wiki/Swap_chain)と呼びます。

多バッファの詳細については以下を参照してください：

- https://en.wikipedia.org/wiki/Multiple_buffering

### DPI

> 画像の1インチの長さ内のピクセル数。
>
> DPI（Dots Per Inch、每英寸点数）はドットマトリックス[デジタル画像](https://baike.baidu.com/item/数码影像/6208065?fromModule=lemma_inlink)に使用される測定単位で、1[インチ](https://baike.baidu.com/item/英寸?fromModule=lemma_inlink)の長さ内のサンプリング、表示または出力点の数を指す。

Windowsでは、**スケーリングとレイアウト**を設定してシステムのDPIを調整します。

![image-20230211105647629](../../../00-C++/Resources/image-20230211105647629.png)

オペレーティングシステムはDPIの変動に応じて一定の設定可能なメカニズムでウィンドウのサイズと内容を調整します。DPI感知の詳細については以下を参照してください：

- https://learn.microsoft.com/zh-cn/windows/win32/hidpi/high-dpi-desktop-application-development-on-windows
- https://doc.qt.io/qt-6/highdpi.html
- https://en.wikipedia.org/wiki/Dots_per_inch

GUI開発過程で、多DPIのプレビューをサポートすることは常に頭痛の種であり、いくつかのパラメータで**整个界面**の**效果とレイアウト**を調整することは難しいからです。

グラフィックス開発過程で、比較的大きな落とし穴は以下の点です：

- Window Size != Buffer Size。多くの人はWindow SizeでFrame Bufferを作成しますが、これは100%スケーリングのコンピュータでは正常に動作しますが、他の場合には問題が発生します。

## Qt基礎

### インストール

https://courses.qt.io/how-to-install-qt/index.html#/lessons/WDj_gIccDJa6gmhH_-3P7uC2QDaZHbYA

![image-20230211151023126](../../../00-C++/Resources/image-20230211151023126.png)

後のグラフィックスの章を学び続けたい場合は、インストール時に`Custom installation`を選択し、以下のモジュールがインストールされていることを確認してください：

- Qt6以上の任意のバージョン、最新版（例えば6.5.2 beta）を推奨し、以下を含む：
  - **MSVC 2019 64-bit**：Visual Studioを使用してQt開発をサポートする
  - **Sources**：Qtソースコード
  - **Qt 5 Compatibility Module**：Qt5の共存モジュール
  - **Qt Shader Tools**：Qtのシェーダーツールで、glslをhlsl、mslに変換するために使用する
  - Qt Debug Infomation Files：オプション、体積が大きく、Qtソースコードのデバッグに使用する
- Developers and Designer Tools
  - **Debugging Tools For Windows**：Windowsデバッグツール
  - **CMake**：ビルドツール、既にある場合はインストール不要
  - Qt Installer Framework：オプション、Qtプログラムをインストーラーにパッケージ化するために使用する

此外、筆者は学習の初期段階で**Designer（*.ui）**および**Design Studio**を使用しないことを推奨します。これらは早期の最大の進歩の障害となります。

### 開始

- `CMakeLists.txt`と`main.cpp`ファイルを新規作成する

- `CMakeLists.txt`に以下を記入する：

  ``` cmake
  cmake_minimum_required(VERSION 3.12)
  project(QtExample)
  
  add_executable(QtExample main.cpp)
  
  find_package(Qt6 COMPONENTS Core Widgets REQUIRED)      #Qt6のモジュールを検索する
  
  target_link_libraries(QtExample                         #CoreとWidgetをQtExampleにリンクする
      PRIVATE 
          Qt6::Core                     
          Qt6::Widgets
  )
  ```

- `main.cpp`に以下を記入する：

  ``` c++
  #include <QApplication>
  #include <QWindow>
  
  int main(int argc, char* argv[]) {
  	QApplication a(argc, argv);
  	QWindow window;
  	window.setTitle("Hello Qt!");
  	window.show();
  	return a.exec();
  }
  ```

- `Cmakeコマンドライン`または`CMake GUI`を使用してVisual Studioプロジェクトを生成する

- **QtExample**をスタートアッププロジェクトとして選択し、コンパイルして実行すると以下が表示されます：

  ![image-20230211170238365](../../../00-C++/Resources/image-20230211170238365.png)

[**QApplication**](https://doc.qt.io/qt-6/qapplication.html)はQtのプログラム全体のスケジューリング管理センターであり、以下のことが可能です：

- プログラムの入力パラメータを解析する（即ち上記の`argument count`、`argument value`）
- オブジェクト間のイベント通信を管理する
- プログラムのイベントループを処理する
- すべてのWindow、Widgetを取得する
- プログラムプロセスに関する各種操作を提供する
- ...

> UEの**GenericApplication**、**SlateApplication**に対応する

`QApplication::instance()`またはマクロ`qApp`を通じて、プログラム内のどこからでも操作を行うことができます。公式ドキュメントを参照すると、何ができるかがわかります：

- https://doc.qt.io/qt-6/qapplication.html

QApplicationはQGuiApplicationを継承し、**Widgetsモジュール**に属します。ウィンドウではなくコンソールプログラムが必要な場合は、**Coreモジュール**の**[QCoreApplication](https://doc.qt.io/qt-6/qcoreapplication.html)**を使用すれば十分です。

> 実際のアプリケーション開発では、QApplicationを派生させて、グローバルな管理操作を追加することができます。

`QApplication::exec()`はQtのイベントループを起動し、以下と同等です：

``` c++
#include <QApplication>
#include <QWindow>

int main(int argc, char* argv[]) {
	QApplication a(argc, argv);	
	Q_ASSERT(QApplication::instance() == &a);

	QWindow window;
	window.setTitle("Hello Qt!");
	window.show();
	while (true) {						//QApplication内部には退出信号があるが、プライベートであるため、ここでは無限ループとする
		QApplication::processEvents();	//これをコメントアウトすると、ウィンドウがインタラクションイベントに応答できなくなる
	}
}
```

> 開発過程では、比較的遅い操作を実行してインターフェースがカクカクする場合があります。この場合はこれらの操作の間に手動で`QApplication::processEvents()`を呼び出してインターフェースをリフレッシュさせることができます。

### Object

Qtの大多数のクラスは[QObject](https://doc.qt.io/qt-6/qobject.html)から派生しており、これによりすべての子オブジェクトを管理し、リフレクション、Signal-Slot、ガベージコレクション、イベントなどのメカニズムを提供します。QObjectサブクラス定義の例は以下の通りです：

``` c++
class QExmapleClass : public QObject {
	Q_OBJECT			//Q_OBJECTはQtリフレクションデータのエントリーであり、リフレクション機能が不要な場合は記述しなくても良い
};
```

#### Meta Object System

QtのメタオブジェクトシステムはSignal-Slot、実行時型情報、動的プロパティシステムなどのメカニズムを提供します。

CMakeをビルドシステムとするプロジェクトでQObjectのリフレクション機能を有効にしたい場合は、`CMakeLists.txt`の`add_executable`の前に[AUTOMOC](https://cmake.org/cmake/help/latest/prop_tgt/AUTOMOC.html)を有効にする必要があります：

``` cmake 
set(CMAKE_AUTOMOC ON)
```

`AUTOMOC`はヘッダーファイルに`Q_OBJECT`のようなマクロが存在するかどうかを自動的に検出し、存在する場合はQtのリフレクションコンパイラ**moc**を使用して該当ファイルをスキャンし、スキャンが終了すると`*.moc`ファイルを生成します。ここにはQtリフレクションデータの定義が含まれています。

以下のようなパスでQtのmocを見つけることができ、現在のディレクトリでcmdを開き、`moc -h`を実行すると、ヘルプドキュメントを見ることができます。

![image-20230211183009226](../../../00-C++/Resources/image-20230211183009226.png)

mocのソースコードは以下のパスにあります：

![image-20230211183213258](../../../00-C++/Resources/image-20230211183213258.png)

> UEのリフレクションコンパイラはUHT（Unreal Header Tool）です。

`*.cpp`に記述された`Q_OBJECT`については、末尾で手動で`*.moc`を`include`する必要があり、これによりmocの実行がトリガーされます。例えば：

```c++
/* main.cpp
*/

#include <QApplication>
#include <QMetaType>
#include <QMetaProperty>

class QExmapleClass : public QObject {
	Q_OBJECT
	Q_PROPERTY(int Var READ getVar WRITE setVar)				//Getter、Setterを通じてMetaPropertyを宣言する
public:
	int getVar() {
		return mVar;
	}
	void setVar(int inVar) {
		mVar = inVar;
	}

	Q_INVOKABLE void sayHello(const QString& inName) {			//MetaMethodを宣言する
		qDebug() << "Hello " << inName.toLatin1().data();
	}

	enum Number {
		Zero, One, Two, Three
	};
	Q_ENUM(Number)												//MetaEnumを宣言する
protected:
	int mVar= 5;
};

int main(int argc, char* argv[]) {
	QApplication a(argc, argv);

	QExmapleClass obj;		

	//MetaObjectを取得する
	const QMetaObject* metaObjectFromStatic = &QExmapleClass::staticMetaObject;	
	const QMetaObject* metaObjectFromVirtual = obj.metaObject();

	//プロパティを読み書きする
	obj.setProperty("Var", QVariant::fromValue<int>(10));						
	QVariant var = obj.property("Var");
	qDebug() << "Property : " << var.value<int>();

	//関数を呼び出す
	qDebug() << "Invoke Method Begin:";
	metaObjectFromStatic->invokeMethod(&obj, "sayHello",Q_ARG(QString,"Boy"));
	qDebug() << "Invoke Method End";

	//MetaType操作
	QMetaType metaType = var.metaType();
	qDebug() << "Meta Type : " << metaType.name() <<" ID: " << metaType.id();

	//MetaObject内でプロパティを走査して読み書きし、プロパティタイプを出力する
	for (int i = metaObjectFromStatic->propertyOffset() ; i < metaObjectFromStatic->propertyCount(); i++) {
		const QMetaProperty& metaProperty = metaObjectFromStatic->property(i);
		metaProperty.write(&obj, QVariant::fromValue<int>(15));
		QVariant varFromMetaObject = metaProperty.read(&obj);
		qDebug() << "Meta Property : " << metaProperty.name() << varFromMetaObject;
	}

	//MetaObject内で関数を走査し、パラメータ情報を出力する
	for (int i = metaObjectFromStatic->methodOffset(); i < metaObjectFromStatic->methodCount(); i++) {	
		const QMetaMethod& metaMethod = metaObjectFromStatic->method(i);
		QByteArrayList paramNames = metaMethod.parameterNames();
		QStringList paramsDefineList;
		for (int j = 0; j < metaMethod.parameterCount(); j++) {
			paramsDefineList << metaMethod.parameterTypeName(j) +" " + paramNames[j];
		}
		qDebug() << QString("Meta Method : %1 %2(%3)")
			.arg(metaMethod.typeName())
			.arg(metaMethod.name())
			.arg(paramsDefineList.join(',')).toLatin1().data();
	}

	//MetaObject内で列挙型を走査し、列挙型情報を出力する
	for (int i = metaObjectFromStatic->enumeratorOffset(); i < metaObjectFromStatic->enumeratorCount(); i++) {
		const QMetaEnum& metaEnum = metaObjectFromStatic->enumerator(i);
		qDebug()<< "Meta Enum : " << metaEnum.name();
		for (int j = 0; j < metaEnum.keyCount(); j++) {
			qDebug() << "-- " << metaEnum.key(j) << " : " << metaEnum.value(j);
		}
	}
	return a.exec();
}

#include "main.moc"		//mocの実行をトリガーするためにmain.mocをincludeする必要がある
```

上記のコードはQtリフレクションの良い例です。更に詳細な内容については以下を参照してください：

- Qt Meta Object System：https://doc.qt.io/qt-6/metaobjects.html
- QMetaObject：https://doc.qt.io/qt-6/qmetaobject.html
- Qt Property System：https://doc.qt.io/qt-6/properties.html

#### Signal-Slot

信号スロットメカニズムはオブジェクト間の通信に使用され、Qtの核心的な特性です。その作用について、疑似コードの例は以下の通りです：

``` c++
class Student{
    Signal nameChanged(string OldName, string NewName);
public:
    void setName(string inName){
        nameChanged.Emit(mName，inName);
        mName = inName;
    }
private:
    string mName;
};

class Tearcher{
public:
    void OnStudentNameChanged(string OldName, string NewName){
        //do something
    }
};

class Class{
protected:    
    void connectEveryone(){
         for(auto Student:mStudents){       
             Student.nameChanged.Connect(mTeacher,&Tearcher::OnStudentNameChanged);
         }
    }
private:
    Teacher mTeacher;
    TArray<Student> mStudents;
};
```

connectEveryone関数を通じて、「先生」がクラスで生徒に「名前を変更したら通知してください」と伝えることができます。

ここではTeacherとStudentの構造を直接変更するのではなく、第三者の集合（クラス）で二者の関係をバインドすることで、良好なデカップリングを実現しています。

> このメカニズムはUEのデリゲート（Delegate）に対応し、Qtの信号スロットより細かいですが、訳文にはいくつかの欠点があります。Signalとも呼ばれると、開発者の連想を引き起こして正しい使用方法を採用することが容易になると信じています。

更に詳細な説明については以下を参照してください：

- https://doc.qt.io/qt-6/signalsandslots.html

#### Event System

Qtでは、イベントは抽象クラス[QEvent](https://doc.qt.io/qt-6/qevent.html)サブクラスのインスタンスオブジェクトを指し、アプリケーション内部または外部の活動の結果を記述するために使用されます。作用上、重いSingle-Slotと見なすことができ、QWigdetのインタラクションメカニズムはすべてイベントを通じて実現されます。

イベントが発生すると、Qtは適切なQEventサブクラスのインスタンスを構築してそれを表し、以下を呼び出します：

- [static bool QCoreApplication::sendEvent(QObject* *receiver*, QEvent event)](https://doc.qt.io/qt-6/qcoreapplication.html#sendEvent)

対応する`QObject`にイベントを送信し、QObjectは虚関数を持っています：

- [virtual bool QObject::event( QEvent* *e* )](https://doc.qt.io/qt-6/qobject.html#event)

`event`をオーバーライドすることで、イベントを処理したり、更に他の関数に分散させたりすることができます。

此外、Qtはイベントリスナーメカニズムをサポートし、QObjectの虚関数をオーバーライドすることができます：

- [bool QObject:: eventFilter ( QObject * *watched* , QEvent* *event* )](https://doc.qt.io/qt-6/qobject.html#eventFilter)

これを`filterObj`とし、以下を呼び出します：

- [void QObject:: installEventFilter ( QObject* *filterObj* )](https://doc.qt.io/qt-6/qobject.html#installEventFilter)

これにより、該当オブジェクトを他のオブジェクトのイベントリスナーとすることができます。

イベントシステムについて詳しくは以下を参照してください：

- https://doc.qt.io/qt-6/eventsandfilters.html

#### Garbage Collection

前節のメモリ管理でQtのGCについて触れました。QtのGC方式は非常にシンプルで、QObjectは唯一のparentを指定することができ、parentが解放されるとchildrenも解放されます。此外、[QObject::deleteLater](https://doc.qt.io/qt-6/qobject.html#deleteLater)を使用してQObjectの解放を遅延させることもできます。

QWidgetでは、子コントロールを追加すると暗黙的にparentが設定され、大多数の場合手動でdeleteする必要はありません。但しparentのライフサイクルを制御する必要があり、オブジェクトツリーの某个ノードに不備があると、サブツリー全体のオブジェクトが解放されなくなります。

### Window

前でWin APIを使用して簡単なウィンドウを作成しましたが、それらのコードは全く**"現代的"**ではありません。大量のマクロ、奇妙な命名規則があります。

**QPlatformWindow**はQtのクロスプラットフォームWindow基盤カプセル化の詳細であり、Qtはこれを一層ラップしてインターフェースを提供しています。即ち[**QWindow**](https://doc.qt.io/qt-6/qwindow.html)です。

> ここはUEの**GenericWindow**と**SWindow**に対応します。
>
> 両者の間に「不倫」がないとは信じられません0.0

Windowのクライアント領域には多くの小さな部品があることが多く、例えば以下のウィンドウ：

![image-20220523202054386](../../../00-C++/Resources/image-20220523202054386.png)

多くのボタンとコントロールを持っています。もし単純にウィンドウのクリックイベント（マウス座標を取得可能）を使用してウィンドウの哪个部分をクリックしたかを判断し、少しずつ描画を処理すると、ウィンドウの複雑さが上がると、コード全体が異常に混乱し、管理が困難になります。WindowsはSubWindowのメカニズムを提供していますが、Windowの維持コストが高いため、多くのフレームワークは**Widget**の概念を提唱しています。

> QtのQWidget、UEのSWidgetに対応します。

Widgetは軽量なWindowと見なすことができ、以下の特性を持っています：

- 表示上、Widgetは1つのブロックと見なすことができ、本質的にはWindowのクライアント領域を分割するものであり、この領域はWidget自身が描画します。
- 論理上、Widgetはツリー状の階層を持ち、イベントは往往にして自顶向下に伝達されます。

### Widget

[**QWidget**](https://doc.qt.io/qt-6/qwidget.html)はQt GUIの核心であり、Qtの大多数の可视部品はこれから派生しています。

その作成も非常に簡単です：

``` c++
#include <QApplication>
#include <QWidget>

int main(int argc, char* argv[]) {
	QApplication a(argc, argv);
	QWidget widget;
	widget.setWindowTitle("Hello QWigdet!");
	widget.show();
	return a.exec();
}
```

単一部品の開発では、主にQWidgetの[Properties](https://doc.qt.io/qt-6/qwidget.html#protected-functions)を設定します：

![image-20230212112016670](../../../00-C++/Resources/image-20230212112016670.png)

そして[Event](https://doc.qt.io/qt-6/qwidget.html#protected-functions)をオーバーライドします：

![image-20230212112135238](../../../00-C++/Resources/image-20230212112135238.png)

これにより、様々な複雑なインターフェース効果とインタラクションロジックを実現します。

#### WindowOption

一般的に、開発者は`QWindow`に接触することさえないことが多いです。最外层（または最上位）の`QWidget`が自動的に`QWindow`を生成するため、このQWidgetにいくつかの設定を行うことでWindowの生成に影響を与えることができます：

- [WindowFlag](https://doc.qt.io/qt-6/qt.html#WindowType-enum)：Windowの标识を調整し、例えばポップアップ、ダイアログ、提示框、无边框など...
- [WindowModality](https://doc.qt.io/qt-6/qt.html#WindowModality-enum)：モーダルウィンドウを設定し、モーダルウィンドウは他のウィンドウの入力をブロックし、一般的にユーザーに現在のオプションを強制的に選択させてから続行するために使用されます。
- [WindowState](https://doc.qt.io/qt-6/qt.html#WindowState-enum)：Windowの状態には`默认`、`最小化`、`最大化`、`全屏（无边框）`、`激活`があり、`setWindowState`を呼び出すことができます。Qtはいくつかの便利なAPIも提供しており、例えば[activateWindow()](https://doc.qt.io/qt-6/qwidget.html#activateWindow)などです。

例えば以下のコード：

```c++
QWidget widget;
widget.setWindowFlags(Qt::FramelessWindowHint|Qt::Tool);
widget.resize(200,200);
widget.show();
```

スクリーンに200*200の灰色の正方形が表示され、下方のタスクバーでそのアイコンも隠されます。

注意すべき点は、Widgetを作成する時にWindowは作成されておらず、Widgetの設定の大多数はそのパラメータ状態機械を調整しているだけであり、イベント実行時にこれらの状態に基づいて実際の操作を行うことです。例えば、showした後でWindowのハンドルを取得することができます。

#### **[Attribute](https://doc.qt.io/qt-6/qt.html#WidgetAttribute-enum)** 

Qt名前空間下には`WA_`で始まる列挙値のセットがあり、[WidgetAttribute](https://doc.qt.io/qt-6/qt.html#WidgetAttribute-enum)を設定するために使用できます。QWidgetに多くの有用な属性を設定することができ、常用されるものは以下の通りです：

- Qt::WA_DeleteOnClose：ウィンドウを閉じる時に該当オブジェクトを破棄する
- Qt::WA_TranslucentBackground：ウィンドウの背景を透明にする

#### Event

GUIの表示はQWidgetの[paintEvent](https://doc.qt.io/qt-6/qwidget.html#paintEvent)にあり、これをオーバーライドすることで、[QPainter](https://doc.qt.io/qt-6/qpainter.html)を使用して任意のグラフィックスを描画することができます。

GUIの本質は、各種インタラクションイベントを借りてプロパティ状態機械を調整し、paintEventの描画結果を切り替えることです。

以下は良い例です：

```c++
#include <QApplication>
#include <QWidget>
#include <QMouseEvent>
#include <QPainter>

class QExmapleWidget:public QWidget {
	Q_OBJECT
private:
	QPoint mMousePressPos;
	bool bIsHovered = false;
protected:
	void mousePressEvent(QMouseEvent* event) override {
		if (event->button() == Qt::LeftButton)		//マウス左ボタンが押された時の位置を記録する
			mMousePressPos = event->pos();			
	}
	void mouseMoveEvent(QMouseEvent* event) override {
		if (event->buttons() & Qt::LeftButton)		//マウス左ボタンを押しながらドラッグする時、Widgetの位置を調整する
			move(event->pos() + this->pos() - mMousePressPos);
	}

	void enterEvent(QEnterEvent* event) override {
		bIsHovered = true;							//マウスがWidgetに入る
		setCursor(Qt::ClosedHandCursor);			//マウスカーソルの形状を設定する
		update();									//インターフェースのリフレッシュをリクエストする
	}

	void leaveEvent(QEvent* event) override {
		bIsHovered = false;							//マウスがWidgetから離れる
		setCursor(Qt::ArrowCursor);					//マウスカーソルの形状を設定する
		update();									//インターフェースのリフレッシュをリクエストする
	}

	void paintEvent(QPaintEvent* event) override {
		QPainter painter(this);

		painter.fillRect(this->rect(),bIsHovered ? Qt::green : QColor(0,100,200));	//領域内に色を埋める

		painter.setPen(QPen(Qt::white));
		painter.setFont(QFont("",20,90));											//デフォルトフォントを使用し、フォントサイズとウェイトを設定する
		painter.drawText(this->rect(), Qt::AlignCenter, "Hello Qt");				//文字を描画する
	}
};

int main(int argc, char* argv[]) {
	QApplication a(argc, argv);
	QExmapleWidget widget;
	widget.setWindowFlags(Qt::FramelessWindowHint);			//无边框
	widget.setAttribute(Qt::WA_TranslucentBackground);		//背景透明		
	widget.setAttribute(Qt::WA_AlwaysStackOnTop);			//置顶
	widget.resize(200,200);
	widget.show();
	return a.exec();
}

#include "main.moc"
```

これを実行すると以下の効果が表示されます：

![8](../../../00-C++/Resources/8.gif)

QWidgetを使用すると、希望する任意の効果を実現することができます。

Qt公式は開発者が使用するために多くの基礎コントロールを提供しています：

- https://doc.qt.io/qt-6/widget-classes.html

![image-20230212115640323](../../../00-C++/Resources/image-20230212115640323.png)

#### Layout

プログラムには往往にしてQWidgetが1つだけではないため、これらのWidgetのレイアウトを組織する必要があります。Qtで常用されるレイアウトはBoxLayoutであり、簡単な例は以下の通りです：

``` c++
#include <QApplication>
#include <QWidget>
#include <QHBoxLayout>
#include <QPushButton>

class QMainWidget :public QWidget {
	Q_OBJECT
public:
	QMainWidget() {
		createUI();
	}
private:
	void createUI() {
		QHBoxLayout* hLayout = new QHBoxLayout(this);
		// QHBoxLayout* hLayout = new QHBoxLayout();			//等价
		//setLayout(hLayout);

		hLayout->setContentsMargins(5, 5, 5, 5);				//外边距を設定する
		hLayout->setSpacing(10);								//内部要素の間隔を設定する
		hLayout->setAlignment(Qt::AlignLeft|Qt::AlignVCenter);	//左对齐且垂直居中を設定する
		hLayout->addWidget(new QPushButton("A"));
		hLayout->addSpacing(20);								//空白填充を加入する
		hLayout->addWidget(new QPushButton("B"));
		hLayout->addWidget(new QPushButton("C"));
	}
};

int main(int argc, char* argv[]) {
	QApplication a(argc, argv);
	QMainWidget widget;
	widget.resize(300, 300);
	widget.show();
	return a.exec();
}

#include "main.moc"
```

![image-20230212120836086](../../../00-C++/Resources/image-20230212120836086.png)

レイアウトの完全な紹介については以下を参照してください：

- https://doc.qt.io/qt-6/layout.html

以下の場合：

- 最外层のWidget
- Layoutが設定されていないが、階層関係を持つWidget

parentの左上角座標に相対的に以下の属性を調整することができます：

- pos：https://doc.qt.io/qt-6/qwidget.html#pos-prop
- geometry：https://doc.qt.io/qt-6/qwidget.html#geometry-prop

同一階層のWidgetは以下のインターフェースを呼び出してZ Orderを調整することができます：

- [raise()](https://doc.qt.io/qt-6/qwidget.html#raise)：置顶
- [lower()](https://doc.qt.io/qt-6/qwidget.html#lower)：置底

#### Style

Qtのデフォルトスタイルは美しくないが、QSSを使用してコントロールを美化することをサポートしています。詳細については以下を参照してください：

- https://doc.qt.io/qt-6/stylesheet.html

## 進階ルート

第三节のC++基礎で述べたように：大量の問題を解くことはプログラミングを学ぶ最良の方法であり、これによりC++の基本構文と各種データ構造とアルゴリズムを迅速に掌握し、自分の誤解を解消し、自分のプログラミング思考を形成することができます。

Qtも同様であり、特定の書籍やチュートリアルに従って順番に学ぶ必要はありません。大量の実践により、某个フレームワークの基本的な骨格を迅速に理解することができます。

フレームワークを学ぶ過程で、最も重要なのは以下の点です：

- 例
- ドキュメント

問題に遭遇した時に往往にして助けになるのは以下の点です：

- コミュニティ

### ドキュメント

**Qt Creator**では、コード内のQt構造を選択して`F1`を押すだけで関連するドキュメントを開くことができます。

**Visual Studio**では、Qt Vs Toolプラグインをインストールする必要があり、設定で以下を有効にします：

![image-20230212132401879](../../../00-C++/Resources/image-20230212132401879.png)

![image-20230212132323526](../../../00-C++/Resources/image-20230212132323526.png)

### 例

以下はQt公式の例チュートリアルページです：

- https://doc.qt.io/qt-6/qtexamplesandtutorials.html

![image-20230212132530234](../../../00-C++/Resources/image-20230212132530234.png)

Qt Creatorにも大量の使用例があります：

![image-20230212132047945](../../../00-C++/Resources/image-20230212132047945.png)

### ソースコード

一定の段階に達した後、好奇心は必ずこのディレクトリを開くように駆り立てます：

![image-20230212132713169](../../../00-C++/Resources/image-20230212132713169.png)

各モジュールの`tests`ディレクトリの下には、実行可能な非常に多くのテストプロジェクトを見つけることができます：

![image-20230212132902145](../../../00-C++/Resources/image-20230212132902145.png)

## 結び

ここまでで、Qtは単なるGUIフレームワークだとまだ思っていますか？

QtのGUI機能はソースコードディレクトリ内の一小部分に過ぎません：

![image-20230212171714634](../../../00-C++/Resources/image-20230212171714634.png)

ソースコードを閲覧すると、Qtがどれほど膨大であるかを体験することができます~

筆者の目から見ると、コードレベルではUnreal Engineに劣っていません。両者は異なる業界に適用されるため、比較する意味はありません。

但し、後の目標がQtであれ、Unreal Engineであれ、その他であれ、方向がC++である限り、Qtは完善なツールライブラリであるだけでなく、コード素養、アーキテクチャ能力を向上させる最良の途でもあります。