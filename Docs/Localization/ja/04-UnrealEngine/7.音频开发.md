---
comments: true
---

# オーディオ開発

長らくレンダリングはグラフィックスエンジンの注目点でしたが、オーディオは開発者の注意を引きにくいように思われます。しかし実際、オーディオは没入型のインタラクティブ体験を高めるだけでなく、画面の表現力をさらに向上させることができます。

<p style="text-align:center"><iframe width="560" height="315" src="//player.bilibili.com/player.html?aid=797879954&bvid=BV1iy4y167pf&cid=259593464&p=1&autoplay=false" scrolling="no" border="0" frameborder="0" framespacing="0" allowfullscreen="true"></iframe></p>

筆者もこれをきっかけにプログラム開発への情熱を燃やし、オーディオにより良いグラフィックス表現効果を与えるために、グラフィックスエンジンの分野に入ったのです。

この記事では、基礎的なオーディオ知識から始め、UE5でよく使われるオーディオインターフェース、そして一般的な**デジタル信号処理（Digital Signal Process）**アルゴリズムを紹介し、最後に**Meta Sound**を取り上げます。

> 筆者は専門のオーディオ開発者ではないため、誤りがあればご指摘ください。

## オーディオを知る

### オーディオとは？

周知のように、音は振動によって生まれます。具体的には次のようになります。

![How Does Sound Travel From One Medium To Another?](../../../04-UnrealEngine/Resources/OIP-C.jpeg)

> 画像元：[How Does Sound Travel From One Medium To Another? (scienceabc.com)](https://www.scienceabc.com/pure-sciences/movement-of-sound-waves-through-different-media.html)

音の本質をよく説明している動画が2つあります。

- [What is Sound-The Dr. Binocs Show](https://www.bilibili.com/video/BV16C4y187Xy)
- [What is Sound-BranchEducation](https://www.bilibili.com/video/BV1pR4y1L7WD)

私たちは通常、音を連続的な波形として認識します。例えば次のように。

![image-20240204102619133](../../../04-UnrealEngine/Resources/image-20240204102619133.png)

コンピュータの記憶精度は有限です（0から1の間には無限の小数が存在します）。音データをコンピュータに保存する場合、2つの次元で精度処理の問題に直面します。

- 時間
- 振幅

この問題を解決するために、**パルス符号変調技術（Pulse Code Modulation、略称PCM）**を使用して、音の**連続信号（Continuous Signal）**を**離散信号（Discrete Signal）**に変換します。

まず、**サンプリング（Sampling）**によって時間次元の精度分割を行います。

![image-20240204103616957](../../../04-UnrealEngine/Resources/image-20240204103616957.png)

時間次元のサンプリング精度パラメータは**サンプリングレート（Sample Rate）**で、単位時間（1秒）内に音信号をサンプリングする回数を表します。一般的なサンプリングレートには`44100 Hz`、`48000Hz`などがあります。

サンプリングデータを取得した後、さらに**量子化（Quantize）**によって振幅次元の精度分割を行います。

![image-20240204104205505](../../../04-UnrealEngine/Resources/image-20240204104205505.png)

振幅次元のサンプリング精度は**ビット深度（Bits Per Sample）**で、1つのオーディオサンプルのbit記憶精度を表します。一般的なビット深度には`8 bit`、`16 bit`、`24 bit`、`32 bit`があります。例えば`8 bit`は`1バイト`に相当し、表現できる数は`256(2^8)`個で、符号位を考慮すると数値範囲は`[-128,127]`となり、128の振幅グラデーションを持つと見なすことができます。

サンプリングと量子化について、優れた記事があります。

- [音视频开发基础入门｜声音的采集与量化、音频数字信号质量、音频码率 - 知乎 (zhihu.com)](https://zhuanlan.zhihu.com/p/577850804)

最終的に得られるデータは通常**ウェーブテーブル/ウェーブグラフ（Wave Table）**と呼ばれます。ビット深度の記憶精度を考慮しない場合、振幅を浮動小数点で表現すると、C++では次のような記憶構造で表すことができます。

``` c++
std::vector<float> WaveTable = {
    sample0,sample1,sample2,sample3,sample4...
}
```

コンピュータのサウンド設定を開くと、各オーディオデバイス（入力または出力）に関連する設定があることがわかります。

![image-20240204105850144](../../../04-UnrealEngine/Resources/image-20240204105850144.png)

現実世界では、**音源（Sound Source）**は周囲に大量の運動粒子を放出します。一部の粒子は直接人間の耳に入り、別の一部は周囲の物体と1回または複数回衝突した後に耳に入ります。このような複雑な周囲環境下では、通常、人間の両耳が異なるオーディオデータを受信するため、オーディオにいわゆる**空間感**が生まれます。

したがって、オーディオをコンピュータに保存する際にこの空間感を保持するために、複数のオーディオデータを保存します。この数を**チャンネル数（Number Of Channels）**と呼びます。チャンネルが1つだけのオーディオを**モノラル（Mono）**、`2つのチャンネル`を持つオーディオを**ステレオ（Stereo）**と呼びます。

複数チャンネルのオーディオサンプルは交差して組み合わされ、1つの組み合わせを**オーディオブロック（Block）**と呼びます。

ステレオの場合、記憶構造は次のようになります。

``` c++
std::vector<float> WaveTable = {
    channel0_sample0, channel1_sample0,    	// Block 0
    channel0_sample1, channel1_sample1,		// Block 1
    channel0_sample2, channel1_sample2,		// Block 2
    channel0_sample3, channel1_sample3,		// Block 3
    channel0_sample4, channel1_sample4,		// Block 4
    ...
}
```

4チャンネルも同様です。

``` c++
std::vector<float> WaveTable = {
    channel0_sample0, channel1_sample0, channel2_sample0, channel3_sample0,		// Block 0
    channel0_sample1, channel1_sample1, channel2_sample1, channel3_sample1,		// Block 1
    channel0_sample2, channel1_sample2, channel2_sample2, channel3_sample2,		// Block 2
    channel0_sample3, channel1_sample3, channel2_sample3, channel3_sample3,		// Block 3
    channel0_sample4, channel1_sample4, channel2_sample4, channel3_sample4,		// Block 4
    ...
}
```

通常、Wave Tableはオーディオデバイスで直接再生することはできません。なぜなら、波形データの塊であり、サンプリングレート、ビット深度、チャンネル数などのオーディオのフォーマット情報が含まれていないためです。

再生可能にするには、さらにオーディオファイルにエンコードする必要があります。

### WAV

`*.WAV`は最も一般的なサウンドファイル形式の1つで、MicrosoftがWindows向けに開発した標準的なデジタルオーディオファイルです。`*.mp3`、`*.flac`、`*.aac`などのオーディオ形式とは異なり、無圧縮のオーディオであり、WaveTableのデータの前にオーディオのフォーマットを保存するだけです。

![The structure of .wav file format. | Download Scientific Diagram](../../../04-UnrealEngine/Resources/The-structure-of-wav-file-format.png)

これに関する完全なドキュメントがあります。

- [WAV文件格式解析及处理 - 简书 (jianshu.com)](https://www.jianshu.com/p/63d7aa88582b)

上記の記事をよく読んで、WAV形式の各データの意味を理解してください。

その後、C++でWAVのファイルヘッダ構造を次のようにメモリアライメントして記述できます。

``` c++
struct WavFmtChunk {
	uint8_t chunkID[4] = { 'f','m','t',' ' };
	int32_t chunkSize;
	int16_t audioFormat;
	int16_t numChannels;
	int32_t sampleRate;
	int32_t byteRate;
	int16_t blockAlign;
	int16_t bitsPerSample;
};

struct WavDataChunkHeader {
	uint8_t chunkID[4] = { 'd','a','t','a' };
	int32_t chunkSize;
};

struct WavRiffChunk {
	uint8_t chunkID[4] = { 'R','I','F','F' };
	int32_t chunkSize;
	uint8_t format[4] = { 'W','A','V','E' };
	WavFmtChunk fmtChunk;
	WavDataChunkHeader dataChunkHeader;
};

typedef WavRiffChunk WavHeader;
```

C++でデータを保存する場合、通常`std::vector<uint8_t>`を使用します。波形データをWavファイルに書き込む場合は、次のような関数を使用できます。

``` c++
void writeWavFile(std::string filePath, int numChannels, int sampleRate, int bitsPerSample, std::vector<uint8_t> waveTable) {
	WavHeader header;
	header.fmtChunk.audioFormat = 0x0001;			// PCM形式に対応する数値は0x0001
	header.fmtChunk.chunkSize = 16;					// PCM形式のオーディオ形式は16バイトを占める。他の形式の場合は16より大きくなり、ファイルヘッダの総サイズが44より大きくなる可能性がある
	header.fmtChunk.numChannels = numChannels;
	header.fmtChunk.bitsPerSample = bitsPerSample;
	header.fmtChunk.sampleRate = sampleRate;
	header.fmtChunk.blockAlign = header.fmtChunk.numChannels * header.fmtChunk.bitsPerSample / 8;
	header.fmtChunk.byteRate = header.fmtChunk.sampleRate * header.fmtChunk.blockAlign;
	header.dataChunkHeader.chunkSize = waveTable.size();
	header.chunkSize = sizeof(header.format) + sizeof(header.fmtChunk) + sizeof(header.dataChunkHeader) + waveTable.size();
	std::ofstream out(filePath, std::ios::out | std::ios::binary);		//必ずstd::ios::binaryを使用する。そうしないとデータが乱れる
	assert(out.is_open() && "Can`t Write File");
	out.write(reinterpret_cast<char*>(&header), sizeof(WavHeader));
	out.write(reinterpret_cast<char*>(waveTable.data()), waveTable.size());
	out.close();
}

```

読み取りも非常に簡単です。

``` c++
std::vector<uint8_t> readWavFile(std::string filePath) {
	WavHeader header;
	std::ifstream in(filePath, std::ios::in | std::ios::binary);				//必ずstd::ios::binaryを使用する。そうしないとデータが乱れる
	assert(in.is_open() && "Can`t Read File");
	in.read(reinterpret_cast<char*>(&header), sizeof(header));
	int waveTableSize = header.dataChunkHeader.chunkSize;						//波形データのサイズを取得
	std::vector<uint8_t> waveTable(waveTableSize);		
	in.read(reinterpret_cast<char*>(waveTable.data()), waveTableSize);
	return waveTable;
}
```

次に、オーディオ波形を生成します。**正弦波（Sine Wave）**は基本的な波形の1つで、その関数式は次のとおりです。
$$
y(Time) = Amplitude * sin(2 * PI * Frequency * Time + PhaseOffset) + Bias
$$

関数式に基づいて、次のような関数を作成できます。

```C++
const double M_PI = 3.1415926535;
std::vector<uint8_t> generateSineWave(int timeLengthSec,	// 正弦波の長さ（単位：秒）
                                            int frequency, 		// 正弦波の周波数
                                            float amplitude,	// 正弦波の振幅
                                            int numChannels, 	// オーディオのチャンネル数
                                            int sampleRate, 	// サンプリングレート
                                            int bitsPerSample	// ビット深度
                                           ) {
	int numBlocks = timeLengthSec * sampleRate;							// オーディオブロックの数
	amplitude = std::clamp(amplitude, 0.01f, 1.0f);						// 振幅の大きさを制限。std::clampはC++17が必要
	std::vector<uint8_t> waveTable;								
	waveTable.resize(numBlocks * numChannels * bitsPerSample / 8);		// オーディオデータに必要なメモリサイズを計算
	int step = bitsPerSample / 8;
	for (int i = 0; i < numBlocks; i++) {
		float time = i / (float)numBlocks * timeLengthSec;		// 時間点
		float sampleValue = amplitude * std::sin(2 * M_PI * frequency * time);		//対応する時間点のサンプル値を計算
        // sampleValueをwaveTableに書き込む方法を考える必要がある
	}
	return waveTable;
}
```

上記のコードには次の難題があります。

- 正弦波の関数によって`float`型のサンプル値が得られますが、ビット深度は`8 bit`、`16 bit`、`24 bit`、`32 bit`の可能性があります。floatを対応するビット深度のデータに変換するにはどうすればよいですか？

一般的な方法は、既存のデータ型を利用して対応するデータビットを割り当てることです。例えば次のように。

``` c++
const int PCMS16MaxAmplitude = 32767;	 // 16 bitの数値範囲は[-32768,32767]。ここでは最小の絶対値（32767）を取り、越境を回避する
void write16BitSampleValue(uint8_t* dataPtr, float sampleValue) {
	(*reinterpret_cast<int16_t*>(dataPtr)) = sampleValue * PCMS16MaxAmplitude;
}
```

ビット演算を利用して埋めることもできます。

``` c++
const int PCMS24MaxAmplitude = 8388607;
void write24BitSampleValue(uint8_t* dataPtr, float sampleValue) {
    int value = sampleValue * PCMS24MaxAmplitude;
    dataPtr[0] = value & 0xFF;
    dataPtr[1] = (value >> 8) & 0xFF;
    dataPtr[2] = (value >> 16) & 0xFF;
}
```

以上をまとめて、次のような変換関数テーブルを整理できます。

``` c++
const int PCMS8MaxAmplitude = 127;
const int PCMS16MaxAmplitude = 32767;
const int PCMS24MaxAmplitude = 8388607;
const int PCMS32MaxAmplitude = 2147483647;

typedef std::function<void(uint8_t*, float)> SampleValueWriteFunction;
static std::map<uint8_t, SampleValueWriteFunction> SampleValueWriteFunctions = {
	{8,[](uint8_t* dataPtr, float sampleValue) {
         // なぜここに偏差を加えると波形が正しくなるのか、筆者は不明=.=
		(*reinterpret_cast<int8_t*>(dataPtr)) = sampleValue * PCMS8MaxAmplitude + PCMS8MaxAmplitude;  
	}},
	{16,[](uint8_t* dataPtr, float sampleValue) {
		(*reinterpret_cast<int16_t*>(dataPtr)) = sampleValue * PCMS16MaxAmplitude;
	}},
	{24,[](uint8_t* dataPtr, float sampleValue) {
		int value = sampleValue * PCMS24MaxAmplitude;
		dataPtr[0] = value & 0xFF;
		dataPtr[1] = (value >> 8) & 0xFF;
		dataPtr[2] = (value >> 16) & 0xFF;
	}},
	{32,[](uint8_t* dataPtr, float sampleValue) {
		(*reinterpret_cast<int32_t*>(dataPtr)) = sampleValue * PCMS32MaxAmplitude;
	}}
};

typedef std::function<float(uint8_t*)> SampleValueReadFunction;
static std::map<uint8_t, SampleValueReadFunction> SampleValueReadFunctions = {
	{8,[](uint8_t* dataPtr) {
		return ((*reinterpret_cast<int8_t*>(dataPtr)) - PCMS8MaxAmplitude) / float(PCMS8MaxAmplitude);
	}},
	{16,[](uint8_t* dataPtr) {
		return (*reinterpret_cast<int16_t*>(dataPtr)) / float(PCMS16MaxAmplitude);
	}},
	{24,[](uint8_t* dataPtr) {
		int value = dataPtr[2];
		value <<= 8;
		value |= dataPtr[1];
		value <<= 8;
		value |= dataPtr[0];
		return value / float(PCMS24MaxAmplitude);
	}},
	{32,[](uint8_t* dataPtr) {
		return (*reinterpret_cast<int32_t*>(dataPtr)) / float(PCMS32MaxAmplitude);
	}}
};
```

以上をまとめ、テストコードを加えると、完全なコードは次のようになります。

``` c++
#include <stdint.h>
#include <assert.h>
#include <map>
#include <vector>
#include <functional>
#include <string>
#include <fstream>
#include <algorithm>
#include <iostream>

struct WavFmtChunk {
	uint8_t chunkID[4] = { 'f','m','t',' ' };
	int32_t chunkSize;
	int16_t audioFormat;
	int16_t numChannels;
	int32_t sampleRate;
	int32_t byteRate;
	int16_t blockAlign;
	int16_t bitsPerSample;
};

struct WavDataChunkHeader {
	uint8_t chunkID[4] = { 'd','a','t','a' };
	int32_t chunkSize;
};

struct WavRiffChunk {
	uint8_t chunkID[4] = { 'R','I','F','F' };
	int32_t chunkSize;
	uint8_t format[4] = { 'W','A','V','E' };
	WavFmtChunk fmtChunk;
	WavDataChunkHeader dataChunkHeader;
};

typedef WavRiffChunk WavHeader;

const int PCMS8MaxAmplitude = 127;
const int PCMS16MaxAmplitude = 32767;
const int PCMS24MaxAmplitude = 8388607;
const int PCMS32MaxAmplitude = 2147483647;

typedef std::function<void(uint8_t*, float)> SampleValueWriteFunction;
static std::map<uint8_t, SampleValueWriteFunction> SampleValueWriteFunctions = {
	{8,[](uint8_t* dataPtr, float sampleValue) {
		(*reinterpret_cast<int8_t*>(dataPtr)) = sampleValue * PCMS8MaxAmplitude + PCMS8MaxAmplitude;   // なぜここに偏差を加えると波形が正しくなるのか、筆者は不明=.=
	}},
	{16,[](uint8_t* dataPtr, float sampleValue) {
		(*reinterpret_cast<int16_t*>(dataPtr)) = sampleValue * PCMS16MaxAmplitude;
	}},
	{24,[](uint8_t* dataPtr, float sampleValue) {
		int value = sampleValue * PCMS24MaxAmplitude;
		dataPtr[0] = value & 0xFF;
		dataPtr[1] = (value >> 8) & 0xFF;
		dataPtr[2] = (value >> 16) & 0xFF;
	}},
	{32,[](uint8_t* dataPtr, float sampleValue) {
		(*reinterpret_cast<int32_t*>(dataPtr)) = sampleValue * PCMS32MaxAmplitude;
	}}
};

typedef std::function<float(uint8_t*)> SampleValueReadFunction;
static std::map<uint8_t, SampleValueReadFunction> SampleValueReadFunctions = {
	{8,[](uint8_t* dataPtr) {
		return ((*reinterpret_cast<int8_t*>(dataPtr)) - PCMS8MaxAmplitude) / float(PCMS8MaxAmplitude);
	}},
	{16,[](uint8_t* dataPtr) {
		return (*reinterpret_cast<int16_t*>(dataPtr)) / float(PCMS16MaxAmplitude);
	}},
	{24,[](uint8_t* dataPtr) {
		int value = dataPtr[2];
		value <<= 8;
		value |= dataPtr[1];
		value <<= 8;
		value |= dataPtr[0];
		return value / float(PCMS24MaxAmplitude);
	}},
	{32,[](uint8_t* dataPtr) {
		return (*reinterpret_cast<int32_t*>(dataPtr)) / float(PCMS32MaxAmplitude);
	}}
};

const double M_PI = 3.1415926535;

std::vector<uint8_t> generateSineWave(int timeLengthSec, int frequency, float amplitude, int numChannels, int sampleRate, int bitsPerSample) {
	int numBlocks = timeLengthSec * sampleRate;							// オーディオブロックの数
	amplitude = std::clamp(amplitude, 0.01f, 1.0f);						// 振幅の大きさを制限。std::clampはC++17が必要
	std::vector<uint8_t> waveTable;								
	waveTable.resize(numBlocks * numChannels * bitsPerSample / 8);
	int step = bitsPerSample / 8;										//各サンプルのバイトスパンを計算
	uint8_t* waveTablePtr = waveTable.data();
	SampleValueWriteFunction writer = SampleValueWriteFunctions.at(bitsPerSample);
	for (int i = 0; i < numBlocks; i++) {
		float time = i / (float)numBlocks * timeLengthSec;				// 現在のオーディオブロックに対応する時間
		float sampleValue = amplitude * std::sin(2 * M_PI * frequency * time);		//現在の時間に対応する正弦値
		for (int channelIndex = 0; channelIndex < numChannels; channelIndex++) {	//すべてのチャンネルで同じ値を使用
			writer(waveTablePtr, sampleValue);
			waveTablePtr += step;
		}
	}
	return waveTable;
}

void writeWavFile(std::string filePath, int numChannels, int sampleRate, int bitsPerSample, std::vector<uint8_t> waveTable) {
	WavHeader header;
	header.fmtChunk.audioFormat = 0x0001;			// PCM形式に対応する数値は0x0001
	header.fmtChunk.chunkSize = 16;					// PCM形式のオーディオ形式は16バイトを占める。他の形式の場合は16より大きくなり、ファイルヘッダの総サイズが44より大きくなる可能性がある
	header.fmtChunk.numChannels = numChannels;
	header.fmtChunk.bitsPerSample = bitsPerSample;
	header.fmtChunk.sampleRate = sampleRate;
	header.fmtChunk.blockAlign = header.fmtChunk.numChannels * header.fmtChunk.bitsPerSample / 8;
	header.fmtChunk.byteRate = header.fmtChunk.sampleRate * header.fmtChunk.blockAlign;
	header.dataChunkHeader.chunkSize = waveTable.size();
	header.chunkSize = sizeof(header.format) + sizeof(header.fmtChunk) + sizeof(header.dataChunkHeader) + waveTable.size();
	std::ofstream out(filePath, std::ios::out | std::ios::binary);		//必ずstd::ios::binaryを使用する。そうしないとデータが乱れる
	assert(out.is_open() && "Can`t Write File");
	out.write(reinterpret_cast<char*>(&header), sizeof(WavHeader));
	out.write(reinterpret_cast<char*>(waveTable.data()), waveTable.size());
	out.close();
}

std::vector<uint8_t> readWavFile(std::string filePath) {
	WavHeader header;
	std::ifstream in(filePath, std::ios::in | std::ios::binary);				//必ずstd::ios::binaryを使用する。そうしないとデータが乱れる
	assert(in.is_open() && "Can`t Read File");
	in.read(reinterpret_cast<char*>(&header), sizeof(header));
	int waveTableSize = header.dataChunkHeader.chunkSize;						//波形データのサイズを取得
	std::vector<uint8_t> waveTable(waveTableSize);		
	in.read(reinterpret_cast<char*>(waveTable.data()), waveTableSize);
	return waveTable;
}

int main() {
	assert(sizeof(WavHeader) == 44 && "WAV Header Size Error");

	int numChannels = 2;			//モノラル
	int sampleRate = 48000;			//サンプリングレートは48000HZ
	int bitsPerSample = 16;			//各オーディオサンプルは16Bit、すなわち2バイトを占める
	int auidoLengthSec = 2;			//2秒の正弦波を生成
	int sineFrequency = 440;		//正弦波の周波数
	float sineAmplitude = 0.8f;		//正弦波の振幅

	std::vector<uint8_t> waveTable = generateSineWave(auidoLengthSec, sineFrequency, sineAmplitude, numChannels, sampleRate, bitsPerSample);

	writeWavFile("./sineWave.wav", numChannels, sampleRate, bitsPerSample, waveTable);

	std::vector<uint8_t> readData = readWavFile("./sineWave.wav");

	assert(readData == waveTable && "Audio Data Error");

	return 0;
}
```

このプログラムを実行すると、出力ディレクトリに`sineWave.wav`オーディオファイルが生成されます。再生してみてください。

![image-20240205181636978](../../../04-UnrealEngine/Resources/image-20240205181636978.png)

パラメータを変更してオーディオを再生成し、異なる周波数の正弦波の違いを聞いてみることもできます。

さらに、空間オーディオの効果を模擬することもできます。

``` c++
std::vector<uint8_t> generateSineSurroundWave(int timeLengthSec, int frequency, float amplitude, int numChannels, int sampleRate, int bitsPerSample) {
	assert(numChannels == 2);

	int numBlocks = timeLengthSec * sampleRate;							// オーディオブロックの数
	amplitude = std::clamp(amplitude, 0.01f, 1.0f);						// 振幅の大きさを制限。std::clampはC++17が必要
	std::vector<uint8_t> waveTable;
	waveTable.resize(numBlocks * numChannels * bitsPerSample / 8);
	int step = bitsPerSample / 8;										//各サンプルのバイトスパンを計算
	uint8_t* waveTablePtr = waveTable.data();
	SampleValueWriteFunction writer = SampleValueWriteFunctions.at(bitsPerSample);

	float leftEarPos[2]	 = { -5,0 };									// 左耳の位置（X，Y）
	float rightEarPos[2] = {  5,0 };									// 右耳の位置（X，Y）
	float radiusOfSurround = 10;										// サラウンドオーディオの半径
	float audioAttenuationFactor = 0.7;									// 音の減衰係数。例えば0.7は、単位長さごとにオーディオが元の0.7倍に減衰することを意味する
	float periodSec = 2;												// 1周するのにかかる時間（単位：秒）。2は2秒ごとに1周することを意味する

	for (int i = 0; i < numBlocks; i++) {
		float time = i / (float)numBlocks * timeLengthSec;							// 現在のオーディオブロックに対応する時間
		float sampleValue = amplitude * std::sin(2 * M_PI * frequency * time);		// 現在の時間に対応する正弦値

		float angle = 2 * M_PI * time / periodSec;									// 現在の回転角度を計算
		float audioSourcePos[2] = {													// 現在の音源の位置を計算
			radiusOfSurround * std::cos(angle),
			radiusOfSurround * std::sin(angle)
		};

		float leftEarDistance  = sqrt(pow(leftEarPos[0] - audioSourcePos[0], 2) + pow(leftEarPos[1] - audioSourcePos[1], 2));		// 音源から左耳までの距離
		float leftSampleValue = sampleValue * pow(audioAttenuationFactor, leftEarDistance);											// 距離に基づいて、減衰後のオーディオサンプル値を計算
		writer(waveTablePtr, leftSampleValue);																						// 左チャンネルに書き込む
		waveTablePtr += step;

		float rightEarDistance = sqrt(pow(rightEarPos[0] - audioSourcePos[0], 2) + pow(rightEarPos[1] - audioSourcePos[1], 2));		// 音源から右耳までの距離
		float rightSampleValue = sampleValue * pow(audioAttenuationFactor, rightEarDistance);										// 距離に基づいて、減衰後のオーディオサンプル値を計算
		writer(waveTablePtr, rightSampleValue);																						// 右チャンネルに書き込む
		waveTablePtr += step;
	}
	return waveTable;
}

int main() {
	assert(sizeof(WavHeader) == 44 && "WAV Header Size Error");
	int numChannels = 2;            //モノラル
	int sampleRate = 48000;         //サンプリングレートは48000HZ
	int bitsPerSample = 16;         //各オーディオサンプルは16Bit、すなわち2バイトを占める
	int auidoLengthSec = 2;         //2秒の正弦波を生成
	int sineFrequency = 150;        //正弦波の周波数
	float sineAmplitude = 0.8f;     //正弦波の振幅
	std::vector<uint8_t> waveTable = generateSineSurroundWave(auidoLengthSec, sineFrequency, sineAmplitude, numChannels, sampleRate, bitsPerSample);
	writeWavFile("./sineWave.wav", numChannels, sampleRate, bitsPerSample, waveTable);
	std::vector<uint8_t> readData = readWavFile("./sineWave.wav");
	assert(readData == waveTable && "Audio Data Error");
	return 0;
}
```

このウェブサイトでは、ファイルをアップロードしてオーディオの波形を表示できます。

- [Musiscope (sciencemusic.org)](https://oscilloscope.sciencemusic.org/)

![image-20240204142115830](../../../04-UnrealEngine/Resources/image-20240204142115830.png)

これで、オーディオがコンピュータ上でどのような原始的な形態を持つかが大まかに理解できたはずです。

## オーディオフレームワーク

### オーディオIOインターフェース

オーディオがあると、オーディオデバイスとのやり取り方法を知る必要があります。一般的なオーディオデバイスには次のものがあります。

- 入力：マイク
- 出力：スピーカー、オーディオ、ヘッドフォン

これらのデバイスメーカーは関連するオーディオドライバを提供またはサポートします。通常、オーディオドライバと直接やり取りする必要はなく、オペレーティングシステムが提供するオーディオインターフェースを利用してオーディオの入出力を処理します。例えば：

- **Windows：** DirectSound、ASIO、WASAPI
- **Linux： ** native ALSA、Jack、 OSS
- **Mac OS：** CoreAudio 、Jack

クロスプラットフォームのオーディオ処理を行うために、多くのフレームワークがこれらのインターフェースを統一的にカプセル化します。例えば：

- **Qt Multimedia** のオーディオIOモジュール：[Audio Overview | Qt Multimedia 6.6.1](https://doc.qt.io/qt-6/audiooverview.html)
- **UE** が導入した **RtAudio** ： http://www.music.mcgill.ca/~gary/rtaudio/

### フーリエ変換ライブラリ

フーリエ変換（Fourier Transform）は信号処理の重要なツールです。

音は時間とともに変化する連続信号であることが知られています。例えば次のように。

![image-20240205162859891](../../../04-UnrealEngine/Resources/image-20240205162859891.png)

しかし、私たちが聞く音は通常1つの周波数だけではなく、実際には多くの周波数の音波が重なっている可能性があります。例えば次のように。

![image-20240205162920488](../../../04-UnrealEngine/Resources/image-20240205162920488.png)

フーリエ変換は、**一定時間内**に大量の異なる周波数の波形が混合された**時間領域信号（Time Domain Signal）**を分析し、その時間帯内の音波形の周波数分布、すなわち**周波数領域信号（Frequency Domain Signal）**を得るのに役立ちます。全体のプロセスは次のようになります。

![TikZ 绘制傅里叶级数示意图 - LaTeX 工作室](../../../04-UnrealEngine/Resources/9796875423a0a9928413d91f8c7420c2.png)

フーリエ変換の原理をよく説明している動画があります。

- [【官方双语】形象展示傅里叶变换 |3Blue1Brown](https://www.bilibili.com/video/BV1pW411J7s8)

いくつかの混乱した時系列信号は、周波数領域に変換するとデータの特徴が見つけやすくなります。例えば、これは音楽のリアルタイム分析結果です。

> 音は聞こえませんが、躍動するグラフから音楽のリズムの変化がわかると思います。

![sfsa](../../../04-UnrealEngine/Resources/sfsa.gif)

さらに、フーリエ変換は単なる数学的処理ツールではなく、周波数領域で問題を考えるように導いてくれます。Shader ToyやGLSL Sandboxを好む場合、多くのコードは周波数領域での思考に基づいています。

各線が固定周波数で均一に回転する面白い動画があります。

- [傅里叶变换之美 | 科学小视](https://www.bilibili.com/video/BV1So4y1T7ae)

成熟したフーリエアルゴリズムの実装がいくつかあります。

- [fftw3](https://github.com/FFTW/fftw3)：西方で最も速いフーリエ変換ライブラリ
- [pffft](https://bitbucket.org/jpommier/pffft)：小型のフーリエ変換ライブラリ
- [ffts](https://github.com/anthonix/ffts)：南方で最も速いフーリエ変換ライブラリ（狗头）

筆者のようにオーディオ可視化が好きな場合は、2つのオープンソースライブラリを推奨します。

- [adamstark/Gist: A C++ Library for Audio Analysis (github.com)](https://github.com/adamstark/Gist)
- [jmerkt/rt-cqt: Real-Time Constant-Q Transform (github.com)](https://github.com/jmerkt/rt-cqt)

### DSPアルゴリズムライブラリ

DSPの全称は **デジタル信号処理（Digital Signal Processor）** です。

「オーディオを知る」のセクションでは、正弦波を生成してwavファイルに書き込みました。通常、これらの機能はDSPライブラリに含まれており、さらに次のような他の機能も提供されます。

- フィルタ
- リサンプリング
- オーディオ合成
- スペクトル分析
- ...

オープンソースのDSPライブラリには次のものがあります。

- [KFR | Fast, modern C++ DSP framework (kfrlib.com)](https://www.kfrlib.com/)
- [Maximilian | C++ Audio and Music DSP Library](https://github.com/micknoise/Maximilian)

### オーディオ開発エンジン

通常、ゲームプロジェクトのオーディオ開発者は、上記のいくつかの低レベルインターフェースとフレームワークを理解する必要はありません。一般的なゲームエンジンは基本的なオーディオモジュールを提供し、複雑なオーディオメカニズムに関しては、オーディオエンジンが提供するエディタを直接使用して開発します。

一般的なオーディオエンジンには次のものがあります。

- [CRIWARE ADX](https://www.criware.com/en/products/adx2.html)：ADXはDAWパラダイムが提供する機能を最大限に活用した作成ツールを備えており、インタラクティブなサウンドを作成するための直感的な機能が追加されているため、学習曲線がほとんどありません。プロジェクトの規模がますます大きくなり、期限がますます厳しくなる中で、ADXを使用して生産性を向上させましょう！
- [FMOD](https://fmod.com/)：FMODは、あらゆるゲームにサウンドと音楽を追加するためのエンドツーエンドのソリューションです。FMOD Studioを使用してアダプティブオーディオを構築し、FMODエンジンを使用してゲーム内で再生します。
- [Wwise | Audiokinetic](https://www.audiokinetic.com/zh/products/wwise/)：インタラクティブなオーディオデザインと開発ツールのセットを提供し、プロジェクトの規模に関係なくプロトタイピングを簡単に行い、その基礎の上でオーディオ作成の構想を現実のものにすることができます。

これらのオーディオソフトウェアは独自のエディタを提供し、関連するゲームエンジン（UE、Unity、Cocos...）でプラグインサポートを提供します。

オーディオエンジンの選択に関しては、知乎に関連する議論があります。

- [有哪些国内的游戏使用了音频引擎来做音频部分的开发？比如FMOD和wwise? - 知乎 (zhihu.com)](https://www.zhihu.com/question/29992299)
- [使用Unity5开发手游，使用FMOD或wwise音频插件而不是自带的声音系统有哪些好处？ - 知乎 (zhihu.com)](https://www.zhihu.com/question/61882604)

**Meta Sound**はUE5が提供する組み込みのオーディオエンジンです。現在の機能は上記のいくつかのオーディオエンジンに匹敵しませんが、このようなワンストップのソリューションは間違いなくUEをますます強力にします。

UE5のモデリングツールと同様に、最大の価値は美術スタッフがUE内でモデリングできるようにすることではなく、本質的に最大の受益者は開発者（TAと言えるでしょうか？）です。

開発者は、自身のプロジェクトのシーンアセットに対して、エディタ下で迅速なアルゴリズム効果検証を行うことができ、戦略が安定した後、UEの低レベルが提供する一連のメッシュ処理アルゴリズムを利用して、DCCモデルから高品質なエンジンアセットへの自動化パイプラインを構築できます。

> 以前のオープンワールドの章で述べたように、HLODはCell内のすべてのモデルを1つの低精度モデルに合成します。複雑なカテゴリを持つシーンで統一的な戦略を使用すると、良い効果が得られにくい場合があります。UEの低レベルのメッシュ処理アルゴリズムに慣れれば、適切なアセット分類があれば、高品質なHLODを生成することは難しくありません。

## UE オーディオ

Unreal Fest 2023で、UEのオーディオアーキテクチャが紹介されました。

- [Sounds Good: Unreal Engine Audio | Unreal Fest 2023](https://www.bilibili.com/video/BV1EC4y1J7Bs)

![image-20240204155736479](../../../04-UnrealEngine/Resources/image-20240204155736479.png)

次のサイトから情報を取得できますが、残念ながらUnrealに関するオーディオのドキュメントは比較的少なく、多くの古いドキュメントが混在しています。

- [在虚幻引擎中使用音频 |虚幻引擎 5.3 文档 (unrealengine.com)](https://docs.unrealengine.com/5.3/en-US/working-with-audio-in-unreal-engine/)
- [Latest unreal-engine topics in Audio - Epic Developer Community Forums (unrealengine.com)](https://forums.unrealengine.com/tags/c/development-discussion/audio/42/unreal-engine)

### オーディオデバイス

オーディオデバイスは入力デバイスと出力デバイスに分かれ、それらの関連情報はそれぞれUEの**Audio::FCaptureDeviceInfo**と**Audio::FAudioPlatformDeviceInfo**に格納されます。次のインターフェースを使用してすべてのデバイスを取得できます。

``` c++
// すべての入力デバイスを取得
Audio::FAudioCapture AudioCapture;
TArray<Audio::FCaptureDeviceInfo> CaptureDevices;
AudioCapture.GetCaptureDevicesAvailable(CaptureDevices);
for (auto InputDeviceInfo : CaptureDevices) {
    UE_LOG(LogTemp, Warning,TEXT("[Input Device] Name: %s, Device Id: %s, Num Channels: %u, Sample Rate: %u, Supports Hardware AEC: %u, "),
        *InputDeviceInfo.DeviceName,
        *InputDeviceInfo.DeviceId,
        InputDeviceInfo.InputChannels,
        InputDeviceInfo.PreferredSampleRate,
        InputDeviceInfo.bSupportsHardwareAEC);
}

// 現在（デフォルト）のオーディオ入力デバイスを取得
Audio::FCaptureDeviceInfo CurrentInputDevice;
AudioCapture.GetCaptureDeviceInfo(CurrentInputDevice);
UE_LOG(LogTemp, Warning, TEXT("[Current Input Device] %s"),
    *CurrentInputDevice.DeviceName);

// すべての出力デバイスを取得
Audio::FMixerDevice* AudioMixerDevice = FAudioDeviceManager::GetAudioMixerDeviceFromWorldContext(GWorld);		//現在のGWorldをWorldContentObjectとして使用
if (AudioMixerDevice){
    if (Audio::IAudioMixerPlatformInterface* MixerPlatform = AudioMixerDevice->GetAudioMixerPlatform()){
        uint32 NumOutputDevices = 0;
        MixerPlatform->GetNumOutputDevices(NumOutputDevices);
        for (uint32 i = 0; i < NumOutputDevices; ++i){
            Audio::FAudioPlatformDeviceInfo OutputDeviceInfo;
            MixerPlatform->GetOutputDeviceInfo(i, OutputDeviceInfo);
            UE_LOG(LogTemp, Warning, TEXT("[Output Device] Name: %s, Device Id: %s, Num Channels: %u, Sample Rate: %u, "),
                *OutputDeviceInfo.Name,
                *OutputDeviceInfo.DeviceId,
                OutputDeviceInfo.NumChannels,
                OutputDeviceInfo.SampleRate);
        }

        // 現在（デフォルト）のオーディオ出力デバイスを取得
        Audio::FAudioPlatformDeviceInfo CurrentOutputDevice = MixerPlatform->GetPlatformDeviceInfo();
        UE_LOG(LogTemp, Warning, TEXT("[Current Output Device] %s"),
            *CurrentOutputDevice.Name);
    }
}
```

![image-20240204170415081](../../../04-UnrealEngine/Resources/image-20240204170415081.png)

### オーディオ出力

UEの音源の基底クラスは**USoundBase**です。

![image-20240204191436270](../../../04-UnrealEngine/Resources/image-20240204191436270.png)

エンジンには多くの派生クラスがあります。

![image-20240204191454472](../../../04-UnrealEngine/Resources/image-20240204191454472.png)

私たちの注目点は主に**USoundWave**にあり、波形データをオーディオデバイスに伝送して再生します。

以前に作成した正弦波生成関数を組み合わせて、UE内でカスタムの波形データを再生することもできます。例えば次のように。

```  c++
void UTestBlueprintLibrary::PlayTestAudio(UObject* WorldContext)		// テスト用のブループリント関数ライブラリ
{
    int numChannels = 2;			//モノラル
    int sampleRate = 48000;			//サンプリングレートは48000HZ
    int bitsPerSample = 16;			//各オーディオサンプルは16Bit、すなわち2バイトを占める
    int auidoLengthSec = 2;			//2秒の正弦波を生成
    int sineFrequency = 440;		//正弦波の周波数
    float sineAmplitude = 0.8f;		//正弦波の振幅

    USoundWaveProcedural* Wave = NewObject<USoundWaveProcedural>();
    Wave->NumChannels = numChannels;
    Wave->SampleByteSize = bitsPerSample / 8;		// UEはint16のみをサポート
    Wave->SetSampleRate(sampleRate);
    std::vector<uint8_t> waveTable = generateSineWave(auidoLengthSec,
                                                      sineFrequency, 
                                                      sineAmplitude, 
                                                      numChannels, 
                                                      sampleRate, 
                                                      bitsPerSample);
    Wave->QueueAudio(waveTable.data(), waveTable.size());
    UGameplayStatics::PlaySound2D(WorldContext, Wave);
}
```

ここでは関数`UGameplayStatics::PlaySound2D`を使用しています。その内部実装を見てみましょう。

``` c++
void UGameplayStatics::PlaySound2D(const UObject* WorldContextObject, 
                                   USoundBase* Sound, 
                                   float VolumeMultiplier,
                                   float PitchMultiplier, 
                                   float StartTime, 
                                   USoundConcurrency* ConcurrencySettings, 
                                   const AActor* OwningActor,
                                   bool bIsUISound)
{
	if (!Sound || !GEngine || !GEngine->UseSound())
	{
		return;
	}

	UWorld* ThisWorld = GEngine->GetWorldFromContextObject(WorldContextObject, EGetWorldErrorMode::LogAndReturnNull);
	if (!ThisWorld || !ThisWorld->bAllowAudioPlayback || ThisWorld->IsNetMode(NM_DedicatedServer))
	{
		return;
	}

	if (FAudioDeviceHandle AudioDevice = ThisWorld->GetAudioDevice())   // 現在のワールドのオーディオ出力デバイスを取得
	{
		FActiveSound NewActiveSound;									// アクティブなオーディオを作成
		NewActiveSound.SetSound(Sound);
		NewActiveSound.SetWorld(ThisWorld);

		NewActiveSound.SetPitch(PitchMultiplier);
		NewActiveSound.SetVolume(VolumeMultiplier);

		NewActiveSound.RequestedStartTime = FMath::Max(0.f, StartTime);

		NewActiveSound.bIsUISound = bIsUISound;
		NewActiveSound.bAllowSpatialization = false;

		if (ConcurrencySettings)
		{
			NewActiveSound.ConcurrencySet.Add(ConcurrencySettings);
		}

		NewActiveSound.Priority = Sound->Priority;
		NewActiveSound.SubtitlePriority = Sound->GetSubtitlePriority();

		// この関数にOwningActorが提供されていない場合、WorldContextObjectから所有者を派生させる
		const AActor* ActiveSoundOwner = OwningActor ? OwningActor : GameplayStatics::GetActorOwnerFromWorldContextObject(WorldContextObject);

		NewActiveSound.SetOwner(ActiveSoundOwner);

		TArray<FAudioParameter> Params;
		UActorSoundParameterInterface::Fill(ActiveSoundOwner, Params);

		AudioDevice->AddNewActiveSound(NewActiveSound, &Params);	// アクティブなオーディオを出力デバイスに渡して処理
	}
}
```

`USoundWaveProcedural::QueueAudio`を呼び出して追加したWaveデータは、内部のAudioBufferに格納され、後でオーディオデバイスが`USoundWaveProcedural::GeneratePCMData`を呼び出して受け取り、音を再生します。

``` c++
int32 USoundWaveProcedural::GeneratePCMData(uint8* PCMData, const int32 SamplesNeeded)
{
    // ...
  	FMemory::Memcpy((void*)PCMData, &AudioBuffer[0], BytesToCopy);
}
```

**USoundWaveProcedural**を使用してPCMデータを再生できますが、複雑なオーディオ生成戦略に関する場合は、**USoundWaveProcedural**を派生させる必要があるかもしれません。**UMetaSoundSource**の実装は很好的参考例であり、**ISoundGenerator**をカスタマイズしてオーディオ生成関数をオーバーライドするだけです。

``` c++
class ISoundGenerator
{
public:
	ENGINE_API ISoundGenerator();
	ENGINE_API virtual ~ISoundGenerator();

	// 新しいバッファが必要なときに呼び出される
	virtual int32 OnGenerateAudio(float* OutAudio, int32 NumSamples) = 0;

	// コールバックごとにレンダリングするサンプル数を返す
	virtual int32 GetDesiredNumSamplesToRenderPerCallback() const { return 1024; }

	// オプション。ジェネレータが生成を開始する直前にオーディオジェネレータスレッドで呼び出される
	virtual void OnBeginGenerate() {}

	// オプション。ジェネレータが生成を終了する直前にオーディオジェネレータスレッドで呼び出される
	virtual void OnEndGenerate() {}

	// オプション。生成が終了したときにサウンドを終了するためにオーバーライドできる
	virtual bool IsFinished() const { return false; }

	// ジェネレータから次のオーディオバッファを取得し、オーディオミキサーから呼び出される
	ENGINE_API int32 GetNextBuffer(float* OutAudio, int32 NumSamples, bool bRequireNumberSamples = false);

	virtual Audio::AudioTaskQueueId GetSynchronizedRenderQueueId() const { return 0; }

protected:

	// オーディオレンダリングスレッドでラムダを実行するための保護されたメソッド
	// パラメータの変更やイベントをジェネレータスレッドに伝達するために使用される
	ENGINE_API void SynthCommand(TUniqueFunction<void()> Command);

private:

	ENGINE_API void PumpPendingMessages();

	// ゲームスレッドからジェネレータスレッドにコマンドを伝達するために使用されるコマンドキュー
	TQueue<TUniqueFunction<void()>> CommandQueue;

	friend class USynthComponent;
};
```

さらに、自身の**USoundWaveProcedural**を派生させてオーバーライドします。

``` c++
virtual ISoundGeneratorPtr USoundBase::CreateSoundGenerator(const FSoundGeneratorInitParams& InParams) { return nullptr; }
```

UEのオーディオデバイスは、オーディオソースを再生するとき、プログラムオーディオ`(USoundWave.bProcedural == true)`である場合、`CreateSoundGenerator`を呼び出して**SoundGenerator**を作成します。

![image-20240204195219544](../../../04-UnrealEngine/Resources/image-20240204195219544.png)

実際にオーディオを解析するときは、次のインターフェースを呼び出してオーディオを生成します。

![image-20240204195409513](../../../04-UnrealEngine/Resources/image-20240204195409513.png)

詳細な実装については、**UMetaSoundSource**と**FMetasoundGenerator**を参照してください。

### オーディオキャプチャ

ゲームプロジェクトではオーディオキャプチャのニーズは少ないため、ここでは簡単に紹介します。

オーディオキャプチャは主に次の2つのタイプに分かれます。

- システムのオーディオをキャプチャする
- ゲーム内のオーディオをキャプチャする

#### システムオーディオキャプチャ

システムオーディオキャプチャは入力デバイス（マイク）の采集を指します。UEは現在出力デバイスのキャプチャをサポートしていません。対応するニーズがある場合は、より低レベルのAPI（WASAPIなど）を使用して実装する必要があります。

``` c++
TSharedPtr<Audio::FAudioCapture> AudioCapture = MakeShared<Audio::FAudioCapture>();
Audio::FOnCaptureFunction OnCapture = [](const float* AudioData,
                                             int32 NumFrames, 
                                             int32 InNumChannels,
                                             int32 InSampleRate, 
                                             double StreamTime, 
                                             bool bOverFlow) {
    // 采集したオーディオデータを処理する。このときは非同期スレッドである
};
Audio::FAudioCaptureDeviceParams Params;									// オーディオ采集のパラメータ設定。デバイスを指定しない場合はデフォルトの入力デバイスを使用する
if (mAudioCapture->OpenCaptureStream(Params, MoveTemp(OnCapture), 1024)) {	// オーディオキャプチャを開始する
    Audio::FCaptureDeviceInfo Info;
    if (mAudioCapture->GetCaptureDeviceInfo(Info)){
        OnAudioFormatChanged.ExecuteIfBound(Info.InputChannels, Info.PreferredSampleRate);  // オーディオ形式変更の通知信号
    }
}
```

#### ゲーム内オーディオキャプチャ

ゲーム内オーディオキャプチャはUEが提供するインターフェース**ISubmixBufferListener**を使用します。

``` c++
/** 特定のサブミックスからオーディオデータを受信するための抽象インターフェース */
class ISubmixBufferListener
{
public:
	virtual ~ISubmixBufferListener() = default;
	
	/**
	特定のサブミックスが新しいバッファをレンダリングしたときに呼び出される
	@param OwningSubmix	新しいバッファをレンダリングしたサブミックスオブジェクト
	@param AudioData		オーディオバッファへのポインタ
	@param NumSamples		オーディオバッファ内のオーディオサンプル数
	@param NumChannels		バッファ内のオーディオのチャンネル数（例：ステレオは2、5.1は6など）
	@param SampleRate		オーディオバッファのサンプリングレート
	@param AudioClock		オーディオレンダリング開始からのダブルオーディオクロック値
	*/
	virtual void OnNewSubmixBuffer(const USoundSubmix* OwningSubmix, float* AudioData, int32 NumSamples, int32 NumChannels, const int32 SampleRate, double AudioClock) = 0;

	/**
	 * FMixerSubmix::IsRenderingAudio()で次のオーディオバッファのためにサブミックスが自身を無効にするか評価する場合に呼び出される
	 * これがtrueを返す場合、FMixerSubmix::IsRenderingAudio()はtrueを返す
	 * それ以外の場合、サブミックスは再生中のサウンド、子サブミックスなどを評価して自動的に無効にするかどうか判断する
	 *
	 * これは毎回評価されるときに呼び出される
	 * 断続的にオーディオをレンダリングするISubmixListenersは、作業がある場合にのみtrueを返す必要がある
	 */
	virtual bool IsRenderingAudio() const
	{
		return false;
	}
};
```

我们只需派生 **ISubmixBufferListener** ，覆写`OnNewSubmixBuffer`处理音频数据，并将其注册到需要捕获 **FAudioDevice** 的 **USoundSubmix** 即可，这块代码依赖比较多，贴在文章中会比较乱，具体的实现大家可参考 **FNiagaraSubmixListener** 。


### DSP 

UE内置了一个非常完善的DSP模块—— **SignalProcessing**  

你几乎能在其中找到你想要的任何东西：  

![image-20240204204639645](../../../04-UnrealEngine/Resources/image-20240204204639645.png)  

这是一些常用的结构和函数：  

- **Audio::TCircularAudioBuffer** ：环形缓冲区，常用于流式音频的存储。  
- **Audio::TSampleBuffer** ：用于将RawPCM数据转换为对应位深的可采样数据，类似于提供之前我们提供的`SampleValueWriteFunction`，不过只支持`int16`和`float`。  
- **Audio::FResampler** ：重采样器，用于修改音频的采样率。  
- **Audio::FWindow** ：用于生成窗函数，支持`Hamming`，`Hann`，`Blackman`。  
- **Audio::TSlidingWindow** ：滑动窗口。  
- **Audio::IFFTAlgorithm** ：提供基础的FFT算法，提供`FFFTFactory::NewFFTAlgorithm(...)`创建。  
- **Audio::FBiquadFilter** ：双二阶滤波器。  
- **Audio::FSpectrumAnalyzer** ：频谱分析仪，支持振幅谱，CQT谱。  
- **Audio::FAsyncSpectrumAnalyzer** ：执行在异步线程的频谱分析仪，功能同上。  
- `ConstantQ.h`：提供了一系列支持常量Q变换的结构，CQT常用于音乐的频谱可视化，它相较于FFT有更好的节奏表现效果。  

对于频谱分析，UE还有一个模块`AudioSynesthesia`来扩展相关的功能。  

![image-20240205104528872](../../../04-UnrealEngine/Resources/image-20240205104528872.png)  

它支持 **CQT** ， **Onset（迸发）** ， **Loudness（响度）** ， **Meter** 的分析，可惜的是大多是一些 **NRT（非实时）** 接口，它要求音频数据必须是在编辑器时导入的资产，不过它里面很多算法结构可以供大家参考。  


### MetaSound  

**Meta Sound** 是UE5推出的新一代音频系统，相较于旧版的 **Sound Cues** ， **Meta Sound** 不仅具有更好的性能， 更重要的是它支持 **程序化 ( Procedural )**  

目前 Meta Sound 在 **UGC** 方面 展现出了非常大的潜力：  

- **Mix Universe** ：[活动演讲 | 将音乐带入《Mix Universe》(官方字幕)](https://www.bilibili.com/video/BV1y84y1a7rr)  
- **Fortnite** ：[堡垒之夜 - AR 编曲](https://www.bilibili.com/video/BV1F94y1L7fW)  

如果你还不了解 Meta Sound，强烈建议去看这个基础视频：  

- [活动演讲 | 初学者的MetaSound指南(官方字幕)](https://www.bilibili.com/video/BV15G4y1A7B5)  

新建一个 **MetaSoundSource** ，创建如下的图表，点击播放，你能听到熟悉的正弦波：  

![image-20240205111218033](../../../04-UnrealEngine/Resources/image-20240205111218033.png)  

Meta Sound  的图表是一种数据流图，这个图每帧都会执行，它的每个节点是一个功能函数块，每个节点会接受Input，并结合MetaSound执行的当前上下文，来填充Output数据，每一帧执行产生的音频会存储到MetaSoundSource 的 AudioBuffer 中，等待音频引擎播放。  

> Trigger Pin 可以简单当成一个bool变量，输出节点会将它置为true，输入节点会读取它的值来判断该节点的逻辑是否要执行  

以上面的正弦波为例，引擎在播放MetaSound时，会经过如下步骤：  

- 首先会创建构建MetaSound图表的异步任务，其对应的函数堆栈如下：  

    ![image-20240205142028396](../../../04-UnrealEngine/Resources/image-20240205142028396.png)  

- 在异步任务中，会加载所有节点（没有依赖的节点会被剔除），并根据依赖关系对剩下的节点进行拓扑排序，相应的代码堆栈如下：  

    ![image-20240205143150293](../../../04-UnrealEngine/Resources/image-20240205143150293.png)  

- 最终会通过节点内部的 **INodeOperatorFactory** 来创建 **IOperator** ， **IOperator** 的部分定义如下，它提供了`BindInput`，`ResetFunction`，`ExecuteFunction`，`PostExecuteFunction`来编写相关逻辑：  

    ``` c++
    class IOperator
    {
    public:
        /** FResetOperatorParams holds the parameters provided to an IOperator's 
         * reset function.
         */
        struct FResetParams
        {
            /** General operator settings for the graph. */
            const FOperatorSettings& OperatorSettings; 
    
            /** Environment settings available. */
            const FMetasoundEnvironment& Environment;
        };
    
        virtual ~IOperator() {}
        
        /** BindInputs binds data references in the IOperator with the FInputVertexInterfaceData.
         *
         * All input data references should be bound to the InVertexData to support
         * other MetaSound systems such as MetaSound BP API, Operator Caching, 
         * and live auditioning. 
         *
         * Note: The virtual function IOPerator::BindInputs(...) will be made a
         * pure virtual when IOperator::GetInputs() is removed at or after release 5.5
         *
         * Note: Binding an data reference may update the which underlying object 
         * the reference points to. Any operator which caches pointers or values 
         * from data references must update their cached pointers in the call to 
         * BindInputs(...).  Operators which do not cache the underlying pointer 
         * of a data reference do not need to update anything after BindInputs(...)
         *
         * Example:
         * 	FMyOperator::FMyOperator(TDataReadReference<float> InGain, TDataReadReference<FAudioBuffer> InAudioBuffer)
         * 	: Gain(InGain)
         * 	, AudioBuffer(InAudioBuffer)
         * 	{
         * 		MyBufferPtr = AudioBuffer->GetData();		
         * 	}
         *
         * 	void FMyOperator::BindInputs(FInputVertexInterfaceData& InVertexData)
         * 	{
         * 		InVertexData.BindReadVertex("Gain", Gain);
         * 		InVertexData.BindReadVertex("Audio", AudioBuffer);
         *
         * 		// Update MyBufferPtr in case AudioBuffer references a new FAudioBuffer.
         * 		MyBufferPtr = AudioBuffer->GetData();
         * 	}
         */
        virtual void METASOUNDGRAPHCORE_API BindInputs(FInputVertexInterfaceData& InVertexData);
    
        /** BindOutputs binds data references in the IOperator with the FOutputVertexInterfaceData.
         *
         * All output data references should be bound to the InVertexData to support
         * other MetaSound systems such as MetaSound BP API, Operator Caching, 
         * and live auditioning. 
         *
         * Note: The virtual function IOPerator::BindOutputs(...) will be made a
         * pure virtual when IOperator::GetOutputs() is removed at or after release 5.5
         *
         * Note: Binding an data reference may update the which underlying object 
         * the reference points to. Any operator which caches pointers or values 
         * from data references must update their cached pointers in the call to 
         * BindOutputs(...).  Operators which do not cache the underlying pointer 
         * of a data reference do not need to update anything after BindOutputs(...)
         *
         * Example:
         * 	FMyOperator::FMyOperator(TDataWriteReference<float> InGain, TDataWriteReference<FAudioBuffer> InAudioBuffer)
         * 	: Gain(InGain)
         * 	, AudioBuffer(InAudioBuffer)
         * 	{
         * 		MyBufferPtr = AudioBuffer->GetData();		
         * 	}
         *
         * 	void FMyOperator::BindOutputs(FOutputVertexInterfaceData& InVertexData)
         * 	{
         * 		InVertexData.BindReadVertex("Gain", Gain);
         * 		InVertexData.BindReadVertex("Audio", AudioBuffer);
         *
         * 		// Update MyBufferPtr in case AudioBuffer references a new FAudioBuffer.
         * 		MyBufferPtr = AudioBuffer->GetData();
         * 	}
         */
        virtual void METASOUNDGRAPHCORE_API BindOutputs(FOutputVertexInterfaceData& InVertexData);
    
        /** Pointer to initialize function for an operator.
         *
         * @param IOperator* - The operator associated with the function pointer.
         */
        using FResetFunction = void(*)(IOperator*, const FResetParams& InParams);
    
        /** Return the reset function to call during graph execution.
         *
         * The IOperator* argument to the FExecutionFunction will be the same IOperator instance
         * which returned the execution function.
         *
         * nullptr return values are valid and signal an IOperator which does not need to be
         * reset.
         */
        virtual FResetFunction GetResetFunction() = 0;
    
        /** Pointer to execute function for an operator.
         *
         * @param IOperator* - The operator associated with the function pointer.
         */
        using FExecuteFunction = void(*)(IOperator*);
    
        /** Return the execution function to call during graph execution.
         *
         * The IOperator* argument to the FExecutionFunction will be the same IOperator instance
         * which returned the execution function.
         *
         * nullptr return values are valid and signal an IOperator which does not need to be
         * executed.
         */
        virtual FExecuteFunction GetExecuteFunction() = 0;
    
        /** Pointer to post execute function for an operator.
         *
         * @param IOperator* - The operator associated with the function pointer.
         */
        using FPostExecuteFunction = void(*)(IOperator*);
    
        /** Return the FPostExecute function to call during graph post execution.
         *
         * The IOperator* argument to the FPostExecutionFunction will be the same IOperator instance
         * which returned the execution function.
         *
         * nullptr return values are valid and signal an IOperator which does not need to be
         * post executed.
         */
        virtual FPostExecuteFunction GetPostExecuteFunction() = 0;
    };
    ```

    ![image-20240205142303554](../../../04-UnrealEngine/Resources/image-20240205142303554.png)  

- 经过上面的流程后，图表已经构建完毕，还记得我们之前提到过，如果 **USoundWave** 的`bProcedural`为`true`，它会创建一个相应的 **ISoundGenerator** ，并在音频引擎的每一帧，调用`ISoundGenerator::OnGenerateAudio`来填充数据，没错， **Meta Sound** 正是这样的流程：  

    ![image-20240205143955462](../../../04-UnrealEngine/Resources/image-20240205143955462.png)  

- 来看看我们的正弦节点在这一帧执行时做了什么：  

    > 每一帧播放，它会根据`DeltaPhase`递增`Phase`，然后从 **FSineWaveTableGenerator** 的`WaveTable`中获取到相关的样本值，填充到输出的AudioBuffer中  

    ![image-20240205150518808](../../../04-UnrealEngine/Resources/image-20240205150518808.png)  

![image-20240205150739219](../../../04-UnrealEngine/Resources/image-20240205150739219.png)  

至此，相信你已经基本搞懂了 **Meta Sound** 的基本原理。  

但要想用好Meta Sound，还需要更多专业的音频知识，这方面笔者也是一个菜鸡，有点爱莫能助~  

它里面提供了很多的函数节点，试一试，电脑又不会爆炸：  

![image-20240205152403078](../../../04-UnrealEngine/Resources/image-20240205152403078.png)  

但有时候并不能完全满足我们的需求，现在，你可以试着做一些自定义的扩展。  


#### 蓝图交互  

**Meta Sound** 与蓝图并不相同，它支持的变量比较有限：  

![image-20240205152805417](../../../04-UnrealEngine/Resources/image-20240205152805417.png)  

你同样也可以在C++中扩展这些变量，只需要找到合适的参考。  

这些参数并没有直接暴露到属性编辑器，想要修改这些变量值有点复杂，我们可以借助 **UAudioComponent** 来简化这个过程，它派生自 **IAudioParameterControllerInterface** ，该接口提供了很多音频参数设置的便捷接口：  

![image-20240205154502428](../../../04-UnrealEngine/Resources/image-20240205154502428.png)  

如果不想使用 **UAudioComponent** ，可以参考相关函数的具体实现。  

这里需要特意提一下 UE 音频系统中的 `Trigger`，它与C++代码中常用的`Delegate`并不相同，我们可以简单把它当作一个bool变量，比如这样的图表：  

![image-20240205155420006](../../../04-UnrealEngine/Resources/image-20240205155420006.png)  

已知每一个节点都会根据依赖关系的拓扑排序来执行，Input在执行时，会将输出的委托`播放时`置为true，在执行`AD Envelope`的时候，会读取输入委托`触发`与之相连的节点（即`播放时`），如果发现它为true才会执行该节点，在`AD Envelope`执行完毕后，会将输出委托`完成时`置为`true`，此时`Output`节点执行时检测到输入委托`完成时`为true，才会通知`MetaSoundSource`结束播放，也就是这里：  

![image-20240205155953096](../../../04-UnrealEngine/Resources/image-20240205155953096.png)  

![image-20240205160115633](../../../04-UnrealEngine/Resources/image-20240205160115633.png)  


#### 运行时构建  

在 [Sounds Good](https://www.bilibili.com/video/BV1EC4y1J7Bs) 中，大佬提到了Meta Sound已经支持了运行时构建 Sound Graph，但是目前而言并不是很完善，还处于测试阶段，在 **MetasoundEngineTest** 中，能找到很多测试代码：  

![image-20240205160406177](../../../04-UnrealEngine/Resources/image-20240205160406177.png)  

考虑到这部分代码后续可能会有较大的变动，因此暂不展开，后续官方版本稳定后再更新本节内容~  

演示中还提到， Meta Sound 将推出大量新的音频控件！！！！！！！！！！！！  

![image-20240205161259486](../../../04-UnrealEngine/Resources/image-20240205161259486.png)