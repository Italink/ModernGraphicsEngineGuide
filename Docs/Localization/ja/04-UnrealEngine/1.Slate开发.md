---
comments: true
---

SlateはUEに提供されるUIフレームワークで、その核心理念は以下の通りです。

-   [Slate アーキテクチャ|Unreal Engine 5.2 ドキュメント ](https://docs.unrealengine.com/5.2/zh-CN/understanding-the-slate-ui-architecture-in-unreal-engine/)

UIに関するいくつかの基礎概念については、以下を参照してください。

-   [現代グラフィックエンジン入門ガイド（七）— GUI](https://zhuanlan.zhihu.com/p/605656730)

## 基礎

UEでSlateを使用するには、3つの核心構造を理解する必要があります。

-   **FSlateApplication** ：グローバルシングルトンで、すべてのUIのスケジューリングセンターです。
-   **SWindow** ：トップレベルウィンドウで、クロスプラットフォームウィンドウのインスタンス（FGenericWindow）を保持し、ウィンドウに関する設定と操作を提供します。
-   **SWidget** ：ウィジェットで、ウィンドウ領域を分割し、自身の領域内のインタラクションと描画イベントを処理します。

### 基本的な使用方法

UEにおける簡単なSlateの使用例は以下の通りです。

```C++
auto Window = SNew(SWindow)							//ウィンドウを作成
    .ClientSize(FVector2D(600, 600))				//ウィンドウサイズを設定
    [												//ウィンドウ内容を埋める
        SNew(SHorizontalBox)						//水平ボックスを作成
        + SHorizontalBox::Slot()					//サブコントロールスロットを追加
        [											
            SNew(STextBlock)						//テキストボックスを作成
            .Text(FText::FromString("Hello"))		//テキストボックスの内容を設定
        ]
        + SHorizontalBox::Slot()					//サブコントロールスロットを追加
        [
            SNew(STextBlock)						//テキストボックスを作成
            .Text_Lambda([](){ 						//テキストボックスの内容を設定	
                return FText::FromString("Slate");
            })		
        ]
	];
FSlateApplication::Get().AddWindow(Window, true);	//このウィンドウを登録し、即座に表示
```

Slateのコードスタイルは以下の通りです。

-   Slate コントロールのクラス名は一般的に `S`で始まります
-   以下の関数でSlateコントロールを快速に構築できます。
    -   `SNew( WidgetType, ... )`：汎用的な構築方式
    -   `SAssignNew( ExposeAs, WidgetType, ... )`：構築完了後、コントロールを`ExposeAs`に代入
    -   `SArgumentNew( InArgs, WidgetType, ... )`：パラメータセットを使用して構築

-   構築のコード式の中で、いくつかの関数でコントロールの構築パラメータを設定できます。これらの関数はいずれもコントロール自身の参照を返すため、チェーンコールが可能です。
    -   `operatpr .` を通じて関数を呼び出し、コントロールのプロパティとイベントを設定できます
    -   **SPanel** のサブクラスについては、`operator +SPanelType::Slot`を使用してサブコントロールのスロットを追加できます 
    -   **SCompoundWidget** `と ` **SPanel::Slot** については、`operator []`を使用してサブコントロールを埋めることができます
    -   Slateのプロパティとイベントについては、複数の方式でバインドできます。例えば上記の`.Text(...)`は静的な値を設定しますが、バインド関数を通じて動的にプロパティ値を取得することもできます。
        -   `Text_Lambda(...)`
        -   `Text_Raw(...)`
        -   `Text_Static(...)`
        -   `Text_UObject(...)`

コントロール開発では、いくつかの経験（合理的であれば存在する）とIDEの補助に依存してコードを記述できます。

![image-20230530153625676](../../../04-UnrealEngine/Resources/image-20230530153625676.png)

SlateはGame UIとEditor UIの両方として使用できます。

Editorでは、以下の方式でUIを追加できます。

```C++
auto Window = SNew(SWindow)							//トップレベルウィンドウが必須
    .Title(FText::FromString("CustomWindow"))		//ウィンドウタイトルを設定
	.ClientSize(FVector2D(600, 600))				//ウィンドウサイズを設定
	[												//ウィンドウ内容を埋める
		SNew(SSpacer)								//自身のコントロール、ここは空白埋めです				
	];

//方法1：このウィンドウを登録し、即座に表示
FSlateApplication::Get().AddWindow(Window, true);	

//方法2：このウィンドウを登録し、表示せず、手動でShowWindowを呼び出して表示
FSlateApplication::Get().AddWindow(Window, false);	//このウィンドウを登録し、即座に表示
Window->ShowWindow();
```

![image-20230530161318939](../../../04-UnrealEngine/Resources/image-20230530161318939.png)

`DockTab`の方式でウィンドウを追加することもできます。

``` c++
// Tabページの生成器を登録
FGlobalTabmanager::Get()->RegisterTabSpawner(FName("CustomTab"),FOnSpawnTab::CreateLambda([](const FSpawnTabArgs& Args){
    return SNew(SDockTab)
        [
        	SNew(SSpacer)
    	];
}));
FGlobalTabmanager::Get()->TryInvokeTab(FTabId("CustomTab"));		//Tabページのアクティブを試行
```

![image-20230530161340764](../../../04-UnrealEngine/Resources/image-20230530161340764.png)

Gameでは、以下の方式でUIを追加できます。

``` C++
GEngine->GameViewport->AddViewportWidgetContent(
		SNew(SSpacer)
);
```

GameViewportには他のインターフェースも利用可能です。

``` c++
class UGameViewportClient : public UScriptViewportClient, public FExec
{
	virtual void AddViewportWidgetContent( TSharedRef<class SWidget> ViewportContent, const int32 ZOrder = 0 );
	virtual void RemoveViewportWidgetContent( TSharedRef<class SWidget> ViewportContent );
	virtual void AddViewportWidgetForPlayer(ULocalPlayer* Player, TSharedRef<SWidget> ViewportContent, const int32 ZOrder);
	virtual void RemoveViewportWidgetForPlayer(ULocalPlayer* Player, TSharedRef<SWidget> ViewportContent);
	void RemoveAllViewportWidgets();
	void RebuildCursors();
};
```

>   詳細は：https://docs.unrealengine.com/5.2/en-US/using-slate-in-game-in-unreal-engine/ を参照してください。

### 基本概念

UI開発には、以下の基礎概念を理解する必要があります。

-   **ウィンドウの基本状態** ：アクティブ（Active）、フォーカス（Focus）、可視（Visible）、モーダル（Modal）、変換（Transform）
-   **レイアウト戦略及び関連概念** ：
    -   ボックスレイアウト（HBox、VBox）、フローレイアウト（Flow）、グリッドレイアウト（Grid）、アンカーレイアウト（Anchors）、オーバーラップレイアウト（Overlap）、キャンバス（Canvas）
    -   パディング（Padding）、マージン（Margin）、スペーシング（Spacing）、アライメント方式（Alignment）
-   **スタイル管理** ：アイコン（Icon）、スタイル（Style）、ブラシ（Brush）
-   **フォント管理** ：フォントタイプ（Font Family）、テキスト幅測定(Font Measure)
-   **サイズ計算** ：コントロールサイズ計算戦略
-   **インタラクションイベント** ：マウス、キーボード、ドラッグ、フォーカスイベント、イベント処理メカニズム、マウスキャプチャ
-   **描画イベント** ：描画要素、領域クリッピング
-   **基本コントロール** ：ラベル（Label）、ボタン（Button）、チェックボックス（Check）、コンボボックス（Combo）、スライダー（Slider）、スクロールバー（Scroll Bar）、テキストボックス（Text）、ダイアログ（Dialog）、カラーピッカー（Color）、メニューバー（Menu Bar）、メニュー（Menu）、ステータスバー（Status Bar）、スクロールパネル（Scroll ）、スタック（切り替え）パネル（Stack/Switcher）、リストパネル（List）、ツリーパネル（Tree）...
-   **国際化** ：テキストのローカライズ翻訳（Localization）

また、UIフレームワークにどのようなグローバルデータと操作があるかを理解し、UEでは入力`Get`、`Set`を使用し、IDEの提示に従って簡単に確認できます。

>   これも一定のコーディング規範に従うことでもたらされるメリットを体現しています。

![image-20230530165544358](../../../04-UnrealEngine/Resources/image-20230530165544358.png)

### SWidget

**SWidget** は Slate UI開発プロセスにおいて、開発者が最も多く接触するタイプであり、すべてのUIコントロールの基底クラスです。

開発者にとっては、その基本的な骨格を理解し、どのプロパティを調整できるかを知る必要があります。

``` c++
TAttribute<bool> EnabledState,  						//有効状態、コントロールとインタラクションできるかどうかを指定、無効の場合はグレー表示
TAttribute<EVisibility> Visibility,						//可視性戦略
TSharedPtr<IToolTip> ToolTip,							//ツールチップコントロール
TAttribute<FText> ToolTipText,							//ツールチップテキスト内容
TAttribute<TOptional<EMouseCursor::Type> > Cursor,		//マウススタイル
float RenderOpacity,									//描画透明度
TAttribute<TOptional<FSlateRenderTransform>> Transform,	//描画変換
TAttribute<FVector2D> TransformPivot,					//描画変換中心
FName Tag,												//タグ
bool ForceVolatile,										//UIを強制的に無効にする
EWidgetClipping Clipping,								//クリッピング戦略
EFlowDirectionPreference FlowPreference,				//UIの流向
TOptional<FAccessibleWidgetData> AccessibleData,		//データを格納
TArray<TSharedRef<ISlateMetaData>> MetaData				//メタデータ
```

>   詳細は：https://docs.unrealengine.com/5.2/zh-CN/slate-ui-widget-examples-for-unreal-engine/ を参照してください。

どの関数を呼び出せるか：

![image-20230530181510368](../../../04-UnrealEngine/Resources/image-20230530181510368.png)

どの関数をオーバーライド（Override）できるか：

![image-20230530183325442](../../../04-UnrealEngine/Resources/image-20230530183325442.png)

### SWindow

**SWindow** は **SCompoundWidget** を継承しています。これは **SWindow** が **SWidget** と同様のコードスタイルを使用できるようにするためであり、以下のプロパティを持っています。

``` c++
EWindowType Type;								//ウィンドウタイプ
FWindowStyle Style;     						//ウィンドウスタイル
FText Title;									//ウィンドウタイトル
float InitialOpacity;							//初期透明度

FVector2D ScreenPosition 						//ウィンドウ座標
FVector2D ClientSize;							//ウィンドウクライアント領域サイズ
TOptional<float> MinWidth;						//最小幅
TOptional<float> MinHeight;						//最小高さ
TOptional<float> MaxWidth;						//最大幅
TOptional<float> MaxHeight;						//最大高さ
FMargin LayoutBorder;							//ウィンドウ内容のマージン
FMargin UserResizeBorder;						//ウィンドウ領域を調整する際の応答距離

EAutoCenter AutoCenter; 						//センタリング戦略
ESizingRule SizingRule;							//ウィンドウサイズ処理戦略
FWindowTransparency SupportsTransparency; 		//ウィンドウ透明度戦略
EWindowActivationPolicy ActivationPolicy;		//アクティベーション処理戦略

bool IsInitiallyMaximized;						//初期表示時に最大化
bool IsInitiallyMinimized;						//初期表示時に最小化
bool IsPopupWindow;								//Pop up ウィンドウかどうか（タスクバーアイコンなし）
bool IsTopmostWindow;							//最前面ウィンドウかどうか
bool FocusWhenFirstShown;						//初回プレビュー時にフォーカスを取得
bool AdjustInitialSizeAndPositionForDPIScale;	//DPIに応じてウィンドウの初期座標とサイズを調整
bool UseOSWindowBorder;							//オペレーティングシステム自身のウィンドウボーダーを使用
bool HasCloseButton;							//閉じるボタンを持つかどうか
bool SupportsMaximize;							//最大化をサポートするかどうか
bool SupportsMinimize;							//最小化をサポートするかどうか
bool ShouldPreserveAspectRatio;					//アスペクト比をロックするかどうか
bool CreateTitleBar;							//タイトルバーを作成するかどうか
bool SaneWindowPlacement;						//ウィンドウを画面内に制約するかどうか
bool bDragAnywhere;								//任意の位置にドラッグできるかどうか
bool bManualManageDPI;							//DPIを手動で調整するかどうか
```

よく使用される関数は以下の通りです。

``` c++
void MoveWindowTo( FVector2D NewPosition );
void ReshapeWindow( FVector2D NewPosition, FVector2D NewSize );
void ReshapeWindow( const FSlateRect& InNewShape );
void Resize( FVector2D NewClientSize );
void MorphToPosition( const FCurveSequence& Sequence, const float TargetOpacity, const FVector2D& TargetPosition );
void MorphToShape( const FCurveSequence& Sequence, const float TargetOpacity, const FSlateRect& TargetShape );

void ShowWindow();
void HideWindow();
void BringToFront( bool bForce = false );
void RequestDestroyWindow();
void EnableWindow( bool bEnable );
void Maximize();
void Restore();
void Minimize();
```

### 開発フロー

Slate開発の大部分の作業内容は以下のようにまとめられます。

-   **コントロールプロパティの設定**
-   **イベントロジックのバインド**
-   **階層構造の組織**

SWidgetは抽象基底クラスであり、UEは異なる使用方式に応じて派生させ、4つのカテゴリに分類しています。

-   **SCompoundWidget** ：ChildSlotを設定できます

    >   Slateが開発者に提供する主な拡張方式で、一般的にこれを継承するのは既存のSWidgetを利用して一連のWidgetを組織するためです。

-   **SPanel** ：SWidgetのコンテナと看做せ、1つまたは複数のChildSlotを含み、SWidgetを追加するために使用されます。

    > Slateはすでに十分な数のSPanelを派生させ、Slateのレイアウトなどの各種操作を組織するために使用しています。一般的には派生させることは少ないです。

- **SLeafWidget** ：リーフコントロールで、ChildSlotを含みません

    > これを派生させる主な目的は、独特な描画とサイズ処理メカニズムを持つコントロールをカスタマイズするためです。

- **SWeakWidget** ：イベント上ではなく、論理的な帰属を定義します。

    > SPanel内のSWidgetはイベント上で階層関係を持ちますが、ボタンをクリックしてメニューを開くなどの特殊な場合があります。メニューはこのボタンに属すると看做せますが、本質的には新しいウィンドウを開くため、イベント伝達上で階層関係は存在しません。そのためSWeakWidgetでこの問題を解決する必要があります。一般的には使用する機会は少ないです。

ほとんどの場合、**SCompoundWidget** を継承するC++クラスを新規作成し、`Construct`関数でサブコントロールを埋めます。例えば以下のようになります。

``` c++
class SCustomWidget: public SCompoundWidget
{
public:
	SLATE_BEGIN_ARGS(SCustomWidget) {}				//Slateパラメータを定義
	SLATE_END_ARGS()
public:
	void Construct(const FArguments& InArgs){		//SNewを使用する本质はこの関数を呼び出すこと
        ChildSlot									//サブコントロールを埋める
        [
            SNew(STextBlock)
            .Text(FText::FromString("This is body"))
        ];
    }
};
```

これで以下のコードでこのコントロールを作成できます。

``` c++
auto MyWidget = SNew(SCustomWidget);
```

**パラメータ（Argument）** を追加したい場合、例えばテキスト内容を設定できるようにする場合は、定義を以下のように変更します。

``` c++
class SCustomWidget : public SCompoundWidget
{
public:
	SLATE_BEGIN_ARGS(SCustomWidget)							
		: _Text(FText::FromString("Default")) {		//パラメータのデフォルト値を初期化、変数名はパラメータ定義時の変数名の前にアンダースコアを加えたもの
		}				
		SLATE_ARGUMENT(FText,Text)					//マクロSLATE_ARGUMENTでパラメータを定義
	SLATE_END_ARGS()
public:
	void Construct(const FArguments& InArgs) {		
		Text = InArgs._Text;						//渡されたパラメータを受け取る
		ChildSlot									
        [
            SNew(STextBlock)
            .Text(Text)								//テキスト内容を渡す
        ];
	}
private:
	FText Text;
};
```

これで以下のコードでコントロール内容を設定できます。

``` c++
auto MyWidget = SNew(SCustomWidget)
    				.Text(FText::FromString("Hello"));
```

もちろん、FArgumentsの方式を使用せず、直接以下のようにすることもできます。

```c++
class SCustomWidget : public SCompoundWidget
{
public:
	SLATE_BEGIN_ARGS(SCustomWidget){}				
	SLATE_END_ARGS()
public:
	void Construct(const FArguments& InArgs, FText InText) {	//直接Constructの関数パラメータから渡す
		Text = InText;						
		ChildSlot									
		[
            SNew(STextBlock)
            .Text(Text)
		];
	}
private:
	FText Text;
};

```

``` c
auto MyWidget = SNew(SCustomWidget,FText::FromString("Hello"));	  //SNewのパラメータリストからTextを渡す
```

上記のパラメータをデリゲートにバインドできるようにしたい場合、Slateの **属性（Attribute）** メカニズムを使用する必要があります。

``` c++
class SCustomWidget : public SCompoundWidget
{
public:
	SLATE_BEGIN_ARGS(SCustomWidget)							
		: _Text(FText::FromString("Default")) {		
		}				
		SLATE_ATTRIBUTE(FText,Text)							//マクロSLATE_ATTRIBUTEでパラメータを定義
	SLATE_END_ARGS()
public:
	void Construct(const FArguments& InArgs) {		
		Text = InArgs._Text;								//渡されたパラメータを受け取る
		ChildSlot									
			[
				SNew(STextBlock)
				.Text(Text)									//テキスト属性を渡す
			];
	}
private:
	TAttribute<FText> Text;									//TAttributeで属性をラップ
};
```

そして以下のコードを使用できます。

```C++
auto MyWidget = SNew(SCustomWidget)
		.Text_Lambda([](){
			return FText::FromString("Hello");
		});
```

上記のテキストが変更されたときに処理を行うなど、コントロールのイベント処理コールバックを追加したい場合、Slateの **イベント（Event）** メカニズムを使用できます。

``` c++
class SCustomWidget : public SCompoundWidget
{
public:
	SLATE_BEGIN_ARGS(SCustomWidget)							
		: _Text(FText::FromString("Default")) {		
		}				
		SLATE_ATTRIBUTE(FText,Text)							
		SLATE_EVENT(FSimpleDelegate, OnTextChanged)	//マクロSLATE_EVENTでイベントを宣言
	SLATE_END_ARGS()
public:
	void Construct(const FArguments& InArgs) {		
		Text = InArgs._Text;	
		OnTextChanged = InArgs._OnTextChanged;		//イベントデリゲートを渡す
		ChildSlot									
		[
			SNew(STextBlock)
			.Text(Text)
		];
	}
	void SetText(FText InText) {
		Text = InText;
		OnTextChanged.ExecuteIfBound();				//デリゲートを実行
	}
	FText GetText(){
		return Text.Get();
	}
private:
	FSimpleDelegate OnTextChanged;					//デリゲートを定義
	TAttribute<FText> Text;
};
```

``` c++
auto MyWidget = SNew(SCustomWidget)
    .Text_Lambda([](){
        return FText::FromString("Hello");
    })
    .OnTextChanged_Lambda([](){						//テキストが変更されたときにログを出力
        UE_LOG(LogTemp,Warning,TEXT("Oh, Text is changed!"));
    });
```

此外、`SLATE_BEGIN_ARGS(...)`と`SLATE_END_ARGS()`の間には他の使用可能なマクロも存在しますが、使用頻度は高くないため、ここでは展開しません。具体的には以下を参照してください。

-   `Engine\Source\Runtime\SlateCore\Public\Widgets\DeclarativeSyntaxSupport.h`

以上、Slateの開発フローは基本的にこのようになります。

### コントロール一覧

ここでは主に **コントロール<--->タイプ** のマッピングを確立し、どのコントロールが存在するかを知ることを目的としています。これらのコントロールを使用する際、UEソースコード内の大量の使用例を参考にできます。またUEは対応するデバッグツールも提供しています。

#### 基礎

- `SButton`：ボタン


![image-20220518154617249](../../../04-UnrealEngine/Resources/image-20220518154617249.png)

- `SCheckBox`：チェックボックス


![image-20220518154711351](../../../04-UnrealEngine/Resources/image-20220518154711351.png)

- `SComboBox<OptionType>`：カスタム要素のコンボボックス、メニューアイテムのコントロール生成器を提供する必要があります


![image-20220518154846902](../../../04-UnrealEngine/Resources/image-20220518154846902.png)

- `STextComboBox` ：テキストコンボボックス


![image-20220518172336987](../../../04-UnrealEngine/Resources/image-20220518172336987.png)

- `SComboButton`：コンボボタン、ComboBoxと異なり、メニューアイテム生成器ではなく、完全なメニューの生成器を提供する必要があります

![image-20220518155900834](../../../04-UnrealEngine/Resources/image-20220518155900834.png)

- `SInputKeySelector`：キー入力セレクター


![image-20220518162532859](../../../04-UnrealEngine/Resources/image-20220518162532859.png)

- `SNumericDropDown<NumType>`：数値ドロップダウンコントロール


![image-20220518163912751](../../../04-UnrealEngine/Resources/image-20220518163912751.png)

- `SNumericEntryBox<NumType>`：数値選択ボックス


![image-20220518163925567](../../../04-UnrealEngine/Resources/image-20220518163925567.png)

- `SRotatorInputBox = SNumericRotatorInputBox<float>`：回転入力ボックス


![image-20220518164200510](../../../04-UnrealEngine/Resources/image-20220518164200510.png)

- `SSlider`：スライダー


![image-20220518171119875](../../../04-UnrealEngine/Resources/image-20220518171119875.png)

- `SSpinBox<NumericType>`：カスタム数値タイプのスピンボックス


![image-20220518171137321](../../../04-UnrealEngine/Resources/image-20220518171137321.png)

- `SVectorInputBox = SNumericVectorInputBox<float, UE::Math::TVector<float>, 3>`：ベクトル入力ボックス


![image-20220518171848647](../../../04-UnrealEngine/Resources/image-20220518171848647.png)

- `SVolumeControl`：音量調整コントロール


![image-20220518171209154](../../../04-UnrealEngine/Resources/image-20220518171209154.png)

##### テキスト

- `STextBlock`：テキスト表示ブロック


![image-20220519151610023](../../../04-UnrealEngine/Resources/image-20220519151610023.png)

- `SRichTextBlock`：リッチテキスト表示ブロック

- `SEditableText`：編集可能なテキスト


![image-20220518160850714](../../../04-UnrealEngine/Resources/image-20220518160850714.png)

- `SInlineEditableTextBlock`：単行テキスト編集ブロック（編集時にのみボーダーを表示）


![image-20220519151600924](../../../04-UnrealEngine/Resources/image-20220519151600924.png)

- `SEditableTextBox`：編集可能なテキストボックス


![image-20220518161209322](../../../04-UnrealEngine/Resources/image-20220518161209322.png)

- `SSuggestionTextBox`：インテリジェント提示と入力履歴を持つテキストボックス


![image-20220518165125687](../../../04-UnrealEngine/Resources/image-20220518165125687.png)

- `SSearchBox`：検索ボックス


![image-20220518164213105](../../../04-UnrealEngine/Resources/image-20220518164213105.png)

- `SMultiLineEditableText`：多行テキスト編集ブロック


![image-20220519151549183](../../../04-UnrealEngine/Resources/image-20220519151549183.png)

- `SMultiLineEditableTextBox`：多行編集可能なテキストボックス


![image-20220518163837277](../../../04-UnrealEngine/Resources/image-20220518163837277.png)

- `SHyperlink`：ハイパーリンク


![image-20220518161727667](../../../04-UnrealEngine/Resources/image-20220518161727667.png)

##### カラー

- `SColorBlock`：単一カラーの表示コントロール


![image-20220518175311526](../../../04-UnrealEngine/Resources/image-20220518175311526.png)

- `SSimpleGradient`：シンプルなグラデーションカラーの表示コントロール、開始カラーと終了カラーの設定のみをサポート


![](../../../04-UnrealEngine/Resources/image-20220518175922270.png)

- `SComplexGradient`：複雑なグラデーションカラーの表示コントロール、任意の数のカラーで構成されるグラデーションをサポート


![image-20220518180009013](../../../04-UnrealEngine/Resources/image-20220518180009013.png)

- `SColorPicker`：単一カラー調整の統合コントロール（モジュール **AppFramework** を導入する必要があります）


![image-20220518181105385](../../../04-UnrealEngine/Resources/image-20220518181105385.png)

##### 画像

- `SImage`：画像表示に使用


![image-20220519100214978](../../../04-UnrealEngine/Resources/image-20220519100214978.png)

##### ナビゲーション	

- `SBreadcrumbTrail`


![image-20220519101516637](../../../04-UnrealEngine/Resources/image-20220519101516637.png)

##### 通知

- `SErrorHint`：エラーアイコン、ToolTipを設定可能


![image-20220519102729140](../../../04-UnrealEngine/Resources/image-20220519102729140.png)

- `SErrorText`：赤色背景のテキストブロック


![image-20220519102738687](../../../04-UnrealEngine/Resources/image-20220519102738687.png)

- `FNotificationInfo`：コントロールではなく、メッセージ通知に使用


![image-20220519102706748](../../../04-UnrealEngine/Resources/image-20220519102706748.png)

```c++
FNotificationInfo NotificationInfo(FText::FromString("FNotificationInfo"));
NotificationInfo.Text = FText::FromString("This Is Notification Info");
NotificationInfo.bFireAndForget = true;
NotificationInfo.ExpireDuration = 100.0f; 
NotificationInfo.bUseThrobber = true;
FSlateNotificationManager::Get().AddNotification(NotificationInfo);
```

- `SProgressBar`：プログレスバー


![image-20220519101724243](../../../04-UnrealEngine/Resources/image-20220519101724243.png)

- `SHeader`：タイトルコントロール


![image-20220519142042405](../../../04-UnrealEngine/Resources/image-20220519142042405.png)

  - `SScrollBar`：スクロールバー


![image-20220519143516261](../../../04-UnrealEngine/Resources/image-20220519143516261.png)

  - `SSpacer`：空白コントロール、一般的にレイアウト内の埋め込みに使用

#### コントロールコンテナ（SPanel）

##### レイアウトとサイズ

- `SVerticalBox`：垂直レイアウト

- `SHorizontalBox`：水平レイアウト

- `SOverlay`：オーバーラップレイアウト

- `SGridPanel`：グリッドレイアウト


![image-20220519141108149](../../../04-UnrealEngine/Resources/image-20220519141108149.png)

- `SUniformGridPanel`：統一されたグリッドサイズを使用するグリッドレイアウトパネル。

- `SWrapBox`：フローレイアウト、配置方向を設定可能、コントロールがサイズを超えると改行。


![image-20220519134544114](../../../04-UnrealEngine/Resources/image-20220519134544114.png)

- `SUniformWrapPanel`：統一されたサイズを使用するフローレイアウト

- `SCanvas`：任意の座標とサイズを使用してChildを配置可能

- `SConstraintCanvas` :静的

- `SSplitter`：ドラッグ可能な分割線でWidget間の分割サイズを調整可能


![image-20220519144844487](../../../04-UnrealEngine/Resources/image-20220519144844487.png)

- `SScrollBox`：Contentにスクロールバーを追加

![image-20220519133723353](../../../04-UnrealEngine/Resources/image-20220519133723353.png)

- `SWidgetSwitcher`：Widget切り替えコントロール、WidgetをすべてSlotに追加し、Slot Indexを設定して対応するWidgetを切り替え表示

![image-20220519144306639](../../../04-UnrealEngine/Resources/image-20220519144306639.png)

- `SDPIScaler`：DPIスケーリング調整に使用

- `SBox`：Contentの固定サイズ、最小サイズまたは最大サイズを制御


![image-20220519133531893](../../../04-UnrealEngine/Resources/image-20220519133531893.png)

- `SScaleBox`：Contentのサイズを統一的にスケーリング


![image-20220519133607430](../../../04-UnrealEngine/Resources/image-20220519133607430.png)

##### ビュー

- `SListView`：リストビュー


![image-20220519104631179](../../../04-UnrealEngine/Resources/image-20220519104631179.png)

- `STileView`：タイルビュー


![image-20220519105945628](../../../04-UnrealEngine/Resources/image-20220519105945628.png)

- `STreeView`：ツリービュー


![image-20220519110629347](../../../04-UnrealEngine/Resources/image-20220519110629347.png)

##### ドッキングウィンドウ

- `SDockTab`


![image-20220519113722921](../../../04-UnrealEngine/Resources/image-20220519113722921.png)

```C++
const TSharedRef<SDockTab> MajorTab = SNew(SDockTab).TabRole(ETabRole::MajorTab);
TabManager = FGlobalTabmanager::Get()->NewTabManager(MajorTab);

TabManager->RegisterTabSpawner(FName("MyDockTab"), FOnSpawnTab::CreateLambda([](const FSpawnTabArgs& Args) {
    TSharedRef<SDockTab> Tab = SNew(SDockTab).Content()[SNew(STextBlock).Text(FText::FromString("MyDockContent"))];
    return Tab;
})).SetDisplayName(FText::FromString("MyDock"))
    .SetMenuType(ETabSpawnerMenuType::Hidden);
TabManager->TryInvokeTab(FName("MyDockTab"));
```

##### その他

- `SBackgroundBlur`：Contentの背景をぼかす


![image-20220519114840338](../../../04-UnrealEngine/Resources/image-20220519114840338.png)

- `SBorder`：Contentにボーダーを追加


![image-20220519133519815](../../../04-UnrealEngine/Resources/image-20220519133519815.png)

- `STooltipPresenter`：ContentにToolTipを追加

- `SExpandableArea`：Contentの展開または収納を許可


![image-20220519142057130](../../../04-UnrealEngine/Resources/image-20220519142057130.png)

- `SFxWidget`
- `SMenuAnchor`：領域にコンテキストメニューを追加

### コード参考

UEは **コントロールリフレクター** を提供し、対応するUIの作成コードを快速に特定できます。その起動ポイントはエディターメインパネルの`トップメニューバー` - `ツール` - `デバッグ` - `コントロールリフレクター`にあります。

![image-20230531094205665](../../../04-UnrealEngine/Resources/image-20230531094205665.png)

また、公式のエンジンソースコードには **SlateViewer** プロジェクトが提供されており、大部分のSlateコントロールの基本的な使用方法が展示されています。

![image-20230531094416657](../../../04-UnrealEngine/Resources/image-20230531094416657.png)

![image-20230531094851667](../../../04-UnrealEngine/Resources/image-20230531094851667.png)

IDEの **コード検索ツール** を利用して、エンジン内の対応するコントロールのパラメータ例を見つけることができます。`Visual Studio`では`ソリューションエクスプローラー`で`クラスの派生関係`を表示し、すべてのコントロールを概観することもできます。

![image-20230531134824751](../../../04-UnrealEngine/Resources/image-20230531134824751.png)

## 高度なカスタマイズ

上記のスキルでUI開発の大部分のニーズを満たすことができますが、複雑なインタラクションメカニズムや描画ロジックに関わる必要がある場合は、カスタムSLeafWidgetが必要になります。

ここに優れた例があり、ドラッグして移動可能なコントロールを実装しています。

![13](../../../04-UnrealEngine/Resources/13.gif)

コードは以下の通りです。

``` c++
#pragma once
#include "Styling/StyleColors.h"
#include "Brushes/SlateColorBrush.h"

class SCustomSlateWidget :public SLeafWidget {
	SLATE_DECLARE_WIDGET(SCustomSlateWidget, SLeafWidget)	//マクロSLATE_DECLARE_WIDGETでSlateコントロールを宣言
public:
	SLATE_BEGIN_ARGS(SCustomSlateWidget) {}
	SLATE_END_ARGS()
	void Construct(const SCustomSlateWidget::FArguments InArgs) {
		SetCursor(EMouseCursor::GrabHand);					//マウスがコントロール領域に入ったときのスタイルを設定
	}
protected:
	FReply OnMouseButtonDown(const FGeometry& MyGeometry, const FPointerEvent& MouseEvent) override {
		if (MouseEvent.IsMouseButtonDown(EKeys::LeftMouseButton)) {
			LastMouseDownPosition = MyGeometry.AbsoluteToLocal(MouseEvent.GetScreenSpacePosition());//マウス位置をスクリーン空間からコントロールのローカル空間に変換
			return FReply::Handled().CaptureMouse(SharedThis(this));								//グローバルなマウス移動の高頻度キャプチャを開始
		}
		return FReply::Handled();
	}
	FReply OnMouseMove(const FGeometry& MyGeometry, const FPointerEvent& MouseEvent) override{
		if (MouseEvent.IsMouseButtonDown(EKeys::LeftMouseButton)) {
			auto Window = FSlateApplication::Get().FindWidgetWindow(SharedThis(this));					//このコントロールのウィンドウを取得
			auto CurrentMousePosition = MyGeometry.AbsoluteToLocal(MouseEvent.GetScreenSpacePosition());
			auto CurrWindowPosition = Window->GetPositionInScreen();
			auto TargetPosition = CurrentMousePosition + CurrWindowPosition - LastMouseDownPosition;	//位置移動を計算
			Window->MoveWindowTo(TargetPosition);
		}
		return FReply::Handled();
	}
	FReply OnMouseButtonUp(const FGeometry& MyGeometry, const FPointerEvent& MouseEvent) override {
		LastMouseDownPosition = FVector2D::ZeroVector;
		return FReply::Handled().ReleaseMouseCapture();											//マウスキャプチャを解放
	}
	int32 OnPaint(const FPaintArgs& Args, const FGeometry& AllottedGeometry, const FSlateRect& MyCullingRect, FSlateWindowElementList& OutDrawElements, int32 LayerId, const FWidgetStyle& InWidgetStyle, bool bParentEnabled) const override {
		static FSlateFontInfo FontInfo = FCoreStyle::GetDefaultFontStyle("Bold",30);
		static const FSlateBrush* Bursh = FAppStyle::GetBrush("WhiteBrush");
		FLinearColor BackgroundColor = IsHovered()? FLinearColor(0.6f, 0.5f, 0.9f) : FLinearColor(0.1f, 0.5f, 0.9f);
		FSlateDrawElement::MakeBox(OutDrawElements, LayerId, AllottedGeometry.ToPaintGeometry(), Bursh, ESlateDrawEffect::None, BackgroundColor);		//背景を描画
		FSlateDrawElement::MakeText(OutDrawElements, LayerId, AllottedGeometry.ToPaintGeometry(), FText::FromString("Hello Slate"), FontInfo);			//文字を描画
		return LayerId;
	}
	FVector2D ComputeDesiredSize(float LayoutScaleMultiplier) const override {
		return FVector2D::ZeroVector;															// 零ベクトルを使用すると親コントロールの内容を埋める
	}
private:
	FVector2D LastMouseDownPosition = FVector2D::ZeroVector;
};

// 以下のコードはcppに配置する必要があります
SLATE_IMPLEMENT_WIDGET(SCustomSlateWidget)							// マクロSLATE_IMPLEMENT_WIDGETでコード実装を生成
void SCustomSlateWidget::PrivateRegisterAttributes(FSlateAttributeInitializer& AttributeInitializer) 	//この関数を実装する必要があります
{
}
```

``` c++
FSlateApplication::Get().AddWindow(
    SNew(SWindow)
    .ClientSize(FVector2D(200,200))					//ウィンドウサイズを200*200に設定
    .SizingRule(ESizingRule::FixedSize)				//ウィンドウサイズを固定
    .SupportsMaximize(false)						//最大化を無効
    .SupportsMinimize(false)						//最小化を無効
    .CreateTitleBar(false)							//タイトルバーを作成しない
    .AutoCenter(EAutoCenter::PreferredWorkArea)		//中央表示
    .IsTopmostWindow(true)							//最前面表示
    [
        SNew(SCustomSlateWidget)
    ]
);
```

カスタム **SLeafWidget** の作業フローは以下の通りです。

-   マクロ **SLATE_DECLARE_WIDGET(WidgetType, ParentType)** を使用してコントロールを宣言
-   マクロ **SLATE_IMPLEMENT_WIDGET(WidgetType)** を使用してcppでコード実装を定義し、以下の関数を実装します。
    -   `void WidgetType::PrivateRegisterAttributes(FSlateAttributeInitializer& AttributeInitializer)`
-   コントロールサイズ計算の関数をオーバーライドします。
    -   `virtual FVector2D SWidget::ComputeDesiredSize(float LayoutScaleMultiplier) const = 0`
-   各種インタラクションイベントをオーバーライドします。
-   コントロールのプロパティステートマシンを調整します。
-   プロパティステートマシンに基づき、`描画関数（OnPaint）`で描画要素を追加します。

### インタラクションイベント

SWidgetは非常に多くのインタラクションイベント（Onで始まる仮想関数）を提供し、開発者がオーバーライドできます。

- `OnFocusReceived`：フォーカスを取得
- `OnFocusLost`：フォーカスを失う
- `OnFocusChanging`：フォーカスが変更される
- `OnKeyChar`：キーボード入力時
- `OnKeyDown`：キーボードキーが押された時
- `OnKeyUp`：キーボードキーが離された時
- `OnMouseButtonDown`：マウスボタンが押された時
- `OnMouseButtonUp`：マウスボタンが離された時
- `OnMouseMove`：マウスが移動した時
- `OnMouseEnter`：マウスがWidget領域に入った瞬間
- `OnMouseLeave`：マウスがWidget領域から離れた瞬間
- `OnMouseButtonDoubleClick`：マウスダブルクリック
- `OnMouseWheel`：マウスホイールがスクロールした時
- `OnDragEnter`：何かをドラッグしてWidget領域に入った瞬間
- `OnDragLeave`：何かをドラッグしてWidget領域から離れた瞬間
- `OnDragOver`：何かをドラッグしてWidget領域を覆っている時
- `OnDrop`：Widget上でドラッグしたものを解放した時
- ...

>   詳細な説明については、SWidgetの定義を参照してください。各イベントの説明とトリガータイミングが記載されています。用法についてはUEソースコード内の対応する使用例を検索できます。

使用説明：

- これらのイベントは **FSlateApplication** が呼び出しを担当し、その関数パラメータには一般的にこのイベントが携帯するすべての情報が含まれています。私たちはそれをオーバーライド（override）し、イベントデータに基づいて自身のロジックを実行するだけです。

- イベントの戻り値の型は **FReply** で、イベントの後続処理を定義するために使用されます。そのコンストラクタはプライベートであるため、以下の静的関数を使用してインスタンスを作成する必要があります。

    -  `FReplay::Handled()`：このイベントが処理されたことを示し、イベントは次のレベルに伝達されません。
    -  `FReplay::Unhandled()`：このコントロールがこのイベントを処理しないことを示し、イベントは次のレベルに伝達されます。

    **FReply** はイベント終了時にいくつかの処理を行うための一連の操作を提供します。

![image-20230531164053994](../../../04-UnrealEngine/Resources/image-20230531164053994.png)

### 描画イベント

SWidgetの実際の描画関数は`Paint`ですが、描画プロセスにはいくつかの固定ステップが存在するため、Slateは描画イベント`OnPaint`を公開し、カスタム描画ロジックを可能にしています。その定義は以下の通りです。

``` c++
virtual int32 OnPaint(const FPaintArgs& Args, 
                      const FGeometry& AllottedGeometry,
                      const FSlateRect& MyCullingRect, 
                      FSlateWindowElementList& OutDrawElements, 
                      int32 LayerId,
                      const FWidgetStyle& InWidgetStyle,
                      bool bParentEnabled) const = 0;
```

パラメータの説明：

- `const FPaintArgs&` Args：描画イベントのパラメータ、描画の現在時間、間隔、親ウィンドウなどを取得できます。
- `const FGeometry& ` AllottedGeometry：このSWidgetに割り当てられた幾何学的なサイズ（親ウィンドウに対して）
- `  const FSlateRect&` MyCullingRect：このSWidgetのクリッピング矩形、描画中にこの矩形に基づいて描画要素を破棄できます。
- `FSlateWindowElementList&` OutDrawElements：これは整个SWindowの描画要素リストで、描画はこのオブジェクトを変更することで行われます。
- `int32` LayerId：描画のLayerID、一般的にUIをレイヤー分けし、同じレイヤーの描画要素を統一的に処理するために使用されます。
- `const FWidgetStyle&` InWidgetStyle：親ウィンドウから渡されたStyle
- `bool` bParentEnabled：親ウィンドウが有効な状態かどうか。

`OnPaint`関数では、主に **FSlateDrawElement** 内の`Make`で始まる多くの静的関数を使用して描画要素を作成します。其中には以下のものがあります。

``` c++
static void MakeBox( //ボックスを描画、Brushを設定することで、カラーブロックまたは画像にできます
	FSlateWindowElementList& ElementList,
	uint32 InLayer,
	const FPaintGeometry& PaintGeometry,
	const FSlateBrush* InBrush,
	ESlateDrawEffect InDrawEffects = ESlateDrawEffect::None,
	const FLinearColor& InTint = FLinearColor::White );

static void MakeRotatedBox(...);		//回転したボックスを描画
static void MakeGradient(...);			//グラデーションのボックスを描画
static void MakeText(...);				//テキストを描画
static void MakeShapedText(...);		//形状テキストを描画
static void MakeLines(...);				//線を描画
static void MakeSpline(...)				//曲線を描画
static void MakeDrawSpaceSpline(...);	 //描画空間で曲線を描画
static void MakeCubicBezierSpline(...);  //三次ベジェ曲線を描画
static void MakeViewport(...)			//FSlateViewportを描画
static void MakeCustom(...);	 		//カスタム要素を描画、要素のRender関数を自行実装可能
static void MakeCustomVerts(...);		//頂点に基づいて描画
static void MakePostProcessPass(...);	//後期フィルタ（ぼかし）を描画
```

これらの関数はUEに非常に多くの使用例があり、関連コードを検索して参考にできます。

>   詳細は：`Runtime\SlateCore\Public\Rendering\DrawElements.h` を参照してください。

#### FSlateBrush

**FSlateBrush** は領域の塗りつぶしを制御するために使用され、インターフェースでは使用頻度が高いため、Slateはいくつかの便利な拡張を提供しています。

![image-20230531165625590](../../../04-UnrealEngine/Resources/image-20230531165625590.png)

- **FSlateBorderBrush** ：ボーダーを塗りつぶす
- **FSlateBoxBrush** ：ボックスブラシ
- **FSlateRoundedBoxBrush** ：角丸矩形ブラシ
- **FSlateColorBrush** ：カラーブラシ
- **FSlateImageBrush** ：画像
    - **FSlateVectorImageBrush** ：ベクター画像
- **FSlateDynamicImageBrush** ：動的に画像を切り替え可能なブラシ
- **FSlateNoResource** ：リソースを持たないブラシ

> 上記のいくつかのブラシは本质的に特定のパラメータで **FSlateBrush** を構築するものです。上記のブラシを組み合わせる必要がある場合、それらの構築パラメータに基づいて自行組み合わせることができます。

UEにはいくつかの組み込みの **FSlateBrush** が提供されており、ソースコード内で`::Get().GetBrush`を検索して関連する使用例を見つけることができます。

![image-20230531180012391](../../../04-UnrealEngine/Resources/image-20230531180012391.png)

##### 例

整个SWidgetを赤色で塗りつぶしたい場合、以下のコードを使用できます。

```c++
int32 SCustomSlateWidget::OnPaint(const FPaintArgs& Args,
                                  const FGeometry& AllottedGeometry,
                                  const FSlateRect& MyCullingRect,
                                  FSlateWindowElementList& OutDrawElements,
                                  int32 LayerId,
                                  const FWidgetStyle& InWidgetStyle,
                                  bool bParentEnabled) const {
    static FSlateColorBrush ColorBrush(FColor(255,0,0));
	FSlateDrawElement::MakeBox(OutDrawElements,
                               LayerId, 
                               AllottedGeometry.ToPaintGeometry(),
                               &ColorBrush,
                               ESlateDrawEffect::None, 
                               ColorBrush.TintColor.GetColor(InWidgetStyle));
	return LayerId;
}
```

#### ESlateDrawEffect

**ESlateDrawEffect** はSlateにいくつかの効果を提供し、以下の値を取ることができます。

``` c++
enum class ESlateDrawEffect : uint8
{
	None					= 0,			//効果なし
	NoBlending			= 1 << 0,			//ブレンディングなし
	PreMultipliedAlpha	= 1 << 1,			//事前乗算Alpha
	NoGamma				= 1 << 2,			//Gamma補正なし
	InvertAlpha			= 1 << 3,			//透明度を反転
	// ^^ These Match ESlateBatchDrawFlag ^^

	NoPixelSnapping		= 1 << 4,			//ピクセルアライメントを無効
	DisabledEffect		= 1 << 5,			//無効状態の効果
	IgnoreTextureAlpha	= 1 << 6,			//テクスチャの透明度を無視

	ReverseGamma			= 1 << 7		//Gammaを逆転
};
```

#### FSlateFontInfo

**FSlateFontInfo** はフォント情報を指定するために使用され、UE内のフォントアセットに対応します。その部分的な定義は以下の通りです。

``` c++
struct SLATECORE_API FSlateFontInfo
{
	/**フォントオブジェクト（UMGまたはSlateウィジェットスタイルアセットから使用される場合に有効） */
	TObjectPtr<const UObject> FontObject;

	/**このフォントをレンダリングする際に使用するマテリアル */
	TObjectPtr<UObject> FontMaterial;

	/**フォントにアウトラインを適用するための設定 */
	FFontOutlineSettings OutlineSettings;

	/**使用する複合フォントデータ（C++のSlateスタイルセットで使用される場合に有効） */
	TSharedPtr<const FCompositeFont> CompositeFont;

	/**デフォルトのタイプフェイスから使用するフォントの名前（Noneの場合は最初のエントリを使用） */
	FName TypefaceFontName;

	/**
	 * フォントサイズはポイント値で測定されます。ポイントからSlateユニットへの変換は96 dpiで行われます。
	 * レイアウトやUIモックアップのプロトタイピングにPhotoshopなどのツールを使用する場合は、デフォルトのdpi測定値を72 dpiから96 dpiに変更してください。
	 */
	int32 Size;

	/**テキスト内のすべての文字間の均一なスペーシング（トラッキング）。 */
	int32 LetterSpacing = 0;

	/**フォントフォールバックレベル。ランタイムのみ、共有されたFSlateFontInfoに設定しないでください。そうすると他の場所のフォントが変更される可能性があります（コピーを作成してください）。 */
	EFontFallback FontFallback;
};
```

UEには専用のフォントエディターがあり、C++で直接構築するのは比較的面倒ですが、UEはいくつかの組み込みの **FSlateFontInfo** も提供しています。例えば：

- `FCoreStyle::Get().GetFontStyle("NormalFont")`
- `FCoreStyle::Get().GetFontStyle("SmallFont")`
- `FCoreStyle::GetDefaultFontStyle("Regular",FontSize);`
- `FCoreStyle::GetDefaultFontStyle("Bold",FontSize)`
- `FCoreStyle::GetDefaultFontStyle("Italic",FontSize);`
- `FCoreStyle::GetDefaultFontStyle("Mono",FontSize);`

##### FontMeasure

ほとんどのGUIフレームワークには **フォント測定（FontMeasure）** の概念があり、特定のフォント形式のテキストの描画幅と高さを測定します。

UEでは以下の方式で測定できます（疑似コード）。

``` c++
FSlateApplication::Get().GetRenderer()->GetFontMeasureService()->Measure(
    const FText& Text,						//テキスト
    const FSlateFontInfo& InFontInfo,		//フォント
    float FontScale							//フォントスケール
) -> FVector2D								//幅（X）と高さ（Y）を返す
```

### 性能最適化

SlateはUIの`Paint`と`Tick`性能を最適化するため、**SInvalidationPanel** コントロールを提供しています。このコントロールは子コントロール内の消費の高い操作をキャッシュさせ、特定の操作によって **キャッシュが無効（Invalidate）** になった場合にのみ再実行されます。

詳細は：[Unreal Engine Slate アーキテクチャの理解 - ポーリングデータフローとデリゲート | Unreal Engine 5.2 ドキュメント ](https://docs.unrealengine.com/5.2/zh-CN/understanding-the-slate-ui-architecture-in-unreal-engine/#轮询数据流和委托) を参照してください。

手動で子コントロールの`Invalidate()`関数を呼び出してキャッシュを無効にすることができます。

``` c++
void SWidget::Invalidate(EInvalidateWidgetReason InvalidateReason);
```

**EInvalidateWidgetReason** の値は以下の通りです。

``` c++
enum class EInvalidateWidgetReason : uint8
{
	None = 0,
	Layout = 1 << 0,		//Widgetの必要なサイズを変更する必要がある場合、レイアウトを無効にしてください。これは高コストな無効操作であるため、小さなウィジェットを再描画するだけの場合は使用しないでください。
	Paint = 1 << 1,				//描画を無効にする
	Volatility = 1 << 2,		//Volatilityが変動した時にトリガー
	ChildOrder = 1 << 3,		//自コントロールを追加または削除した時にトリガー、無効にするとChildの順序が再構築されます
	RenderTransform = 1 << 4,	//RenderTransformが変動した時にトリガー
	Visibility = 1 << 5,		//可視性が変動した時にトリガー
	AttributeRegistration = 1 << 6,		//属性をバインドした時にトリガー
	Prepass = 1 << 7,					//Prepassを無効にする

	/**
	 * 描画またはサイズに関連する通常のプロパティを変更する場合は、Paintの無効化を使用してください。
	 * さらに、変更されたプロパティがVolatilityに影響を与える場合、Volatilityを無効にして再計算しキャッシュすることが重要です。
	 */
	PaintAndVolatility = Paint | Volatility,
	/**
	 * 描画またはサイズに関連する通常のプロパティを変更する場合は、Layoutの無効化を使用してください。
	 * さらに、変更されたプロパティがVolatilityに影響を与える場合、Volatilityを無効にして再計算しキャッシュすることが重要です。
	 */
	LayoutAndVolatility = Layout | Volatility,
};
```

Slateは **属性（SlateAttribue）** に基づく方式を提供し、属性の変動をキャッシュの再構築に関連付けています。

具体的な方法は：

-   **TSlateAttribute** で属性をラップします（**TAttribute** の用途とは異なります）。**TSlateAttribute** でラップされた属性は、構築初期化時にパラメータ`(*this)`を渡す必要があります。

-   `void WidgetType::PrivateRegisterAttributes(...)`関数で、マクロ **SLATE_ADD_MEMBER_ATTRIBUTE_DEFINITION(_Initializer, _Property, _Reason)** を使用して属性を登録します。

ここに簡単な例があります。

``` c++
class SCustomSlateWidget :public SLeafWidget {
	SLATE_DECLARE_WIDGET(SCustomSlateWidget, SLeafWidget)
public:
	SLATE_BEGIN_ARGS(SCustomSlateWidget){}
	SLATE_END_ARGS()
	void Construct(const SCustomSlateWidget::FArguments InArgs){			
	}
protected:
	SCustomSlateWidget()
		: ClickedCounter(*this) {	//SlateAttributeを初期化
	}
	FReply OnMouseButtonDown(const FGeometry& MyGeometry, const FPointerEvent& MouseEvent) override {
		ClickedCounter.Set(*this, ClickedCounter.Get() + 1);		//カウンター+1、ここではマウスダウンをクリックと看做す
		return FReply::Handled();
	}
	int32 OnPaint(const FPaintArgs& Args, const FGeometry& AllottedGeometry, const FSlateRect& MyCullingRect, FSlateWindowElementList& OutDrawElements, int32 LayerId, const FWidgetStyle& InWidgetStyle, bool bParentEnabled) const override {
		static FSlateFontInfo FontInfo = FCoreStyle::GetDefaultFontStyle("Bold",30);
		FSlateDrawElement::MakeText(OutDrawElements, LayerId, AllottedGeometry.ToPaintGeometry(), FText::FromString(FString::FromInt(ClickedCounter.Get())), FontInfo);					//現在のマウスクリック回数を描画
		return LayerId;
	}
	FVector2D ComputeDesiredSize(float LayoutScaleMultiplier) const override {
		return FVector2D::ZeroVector;															
	}
private:
	TSlateAttribute<int> ClickedCounter;	 	//マウスクリック回数を記録するカウンター
};
```

``` c++
SLATE_IMPLEMENT_WIDGET(SCustomSlateWidget)
void SCustomSlateWidget::PrivateRegisterAttributes(FSlateAttributeInitializer& AttributeInitializer) {
    //ClickedCounterの値の変動を再描画イベントに関連付ける
	//SLATE_ADD_MEMBER_ATTRIBUTE_DEFINITION(AttributeInitializer, ClickedCounter, EInvalidateWidgetReason::Paint);
}
```

``` c++
FSlateApplication::Get().AddWindow(
    SNew(SWindow)
    .SupportsMaximize(false)
    .SupportsMinimize(false)
    .IsTopmostWindow(true)
    .ClientSize(FVector2D(200,200))
    .SizingRule(ESizingRule::FixedSize)
    .CreateTitleBar(false)
    .AutoCenter(EAutoCenter::PreferredWorkArea)
    [
        SNew(SInvalidationPanel)			// SInvalidationPanelでラップする
        [
            SNew(SCustomSlateWidget)
        ]
    ]
);
```

このコードはクリック可能な正方形を作成します：

![image-20230531182616000](../../../04-UnrealEngine/Resources/image-20230531182616000.png)

正方形をクリックすると`ClickedCounter`が+1されますが、画面には何も変化が発生しません。`PrivateRegisterAttributes`内のコメントアウトされたコードを解除すると：

``` c++
void SCustomSlateWidget::PrivateRegisterAttributes(FSlateAttributeInitializer& AttributeInitializer) {
    // ClickedCounterの値の変動を再描画イベントに紐づける
	SLATE_ADD_MEMBER_ATTRIBUTE_DEFINITION(AttributeInitializer, ClickedCounter, EInvalidateWidgetReason::Paint);
}
```

プログラムを再起動すると、`ClickedCounter`が変更されたときにUIが再描画されることが確認できます：

![53](../../../04-UnrealEngine/Resources/53.gif)

## SlateからUMGへ

UEのロジックプログラミングはUObject上に構築されていますが、Slateは極限のパフォーマンスを追求するため、UObjectのような重いメカニズムを使用してUIオブジェクトを管理していません。

Slateでは**共有ポインタ（Shared Pointer）** の方式でUIオブジェクトのライフサイクルを管理しています。

ゲーム開発者のUI開発効率を向上させるため、UEはUObjectを使用してSlateコントロールをラップし、リフレクションを通じてSlateコントロールの一部のプロパティと関数を公開すると同時に、視覚的なUIデザインをサポートしています。このレイヤーのインターフェースが、私たちがよく知る**UMG**です。

**UMG**については、公式ドキュメントに詳細な説明があります：

-   https://docs.unrealengine.com/5.2/zh-CN/umg-ui-designer-for-unreal-engine/

以下の簡潔なコードは、SlateとUMGの関係をよく示しています：

``` c++
#pragma once

#include "UObject/ObjectMacros.h"
#include "Widgets/Text/STextBlock.h"
#include "Components/Widget.h"
#include "CustomUWidget.generated.h"

UCLASS(meta = (DisplayName = "CustomWidget"))					// DisplayNameはコントロール名
class UMGEXTENDUTILITIES_API UCustomWidget : public UWidget
{
	GENERATED_BODY()
protected:
	virtual TSharedRef<SWidget> RebuildWidget() override {						// コントロールを再構築し、メンバー変数に代入する
		return SAssignNew(MyTextBlock,STextBlock);
	}
	virtual void SynchronizeProperties() override {								// UObjectのプロパティをSlateコントロールに同期する
		MyTextBlock->SetText(Text);
		Super::SynchronizeProperties();
	}
	virtual void ReleaseSlateResources(bool bReleaseChildren) override {		// Slateリソースを解放する
		MyTextBlock.Reset();
	}

#if WITH_EDITOR
	virtual const FText GetPaletteCategory() override {							// エディタ上のコントロールカテゴリ
		return FText::FromString("CustomCategory");
	}
#endif

public:
	UFUNCTION(BlueprintCallable)		// 関数をBlueprintに公開する
	FText GetText() const {
		return Text;
	}
	UFUNCTION(BlueprintCallable)
	void SetText(FText InText) {
		Text = InText;
		if (MyTextBlock) {				// プロパティを設定し、コントロールが既に作成されている場合は直接値をコントロールに設定する
			MyTextBlock->SetText(Text);
		}
	}
protected:
	UPROPERTY(EditAnywhere, BlueprintGetter = "GetText", BlueprintSetter = "SetText")
	FText Text;							// プロパティをエディタに公開する

	TSharedPtr<STextBlock> MyTextBlock;	// 実際のSlateコントロール
};
```

![image-20230601182230193](../../../04-UnrealEngine/Resources/image-20230601182230193.png)

C++でUMGを使用する方法は以下の通りです：

``` c++
UWidget* UMGWidget = LoadObject<UWidget>(nullptr, TEXT("{AssetPath}"));    			   // アセットからロード、NewObjectでも可
TSharedPtr<SWidget> SlateWidget = UMGWidget->TakeWidget();                             // この操作で新しいSWidgetが作成される
TSharedPtr<SWidget> CachedSlateWidget = UMGWidget->GetCachedWidget();                  // 前回作成されたSWidgetを取得する
// その後はSlateの使用方法に従う
```

### ライフサイクル管理

**UWidget**は**UObject**のガベージコレクション（GC）に従い、**SWidget**の共有ポインタを保持しています。コントロールが表示されている間（`TSharedPtr<SWidget>`が存在する間）、**UWidget**がGCされないようにするため、UMGは**FGCObject**を継承したコントロール**SObjectWidget**を使用して**UWidget**への参照を追加しています。

![image-20230602103618309](../../../04-UnrealEngine/Resources/image-20230602103618309.png)

上記のサンプルコードに戻ると、`UWidget::TakeWidget()`を呼び出して取得するのは`UWidget::RebuildWidget()`で作成されたコントロールではなく、それをさらに**SObjectWidget**でラップしたものであることがわかります。それらの作成過程における階層関係は以下の通りです：

![图片2](../../../04-UnrealEngine/Resources/%E5%9B%BE%E7%89%872.png)

破棄時の解放順序は以下の通りです：

![图片3](../../../04-UnrealEngine/Resources/%E5%9B%BE%E7%89%873.png)

-   **SObjectWidget**は**FGCObject**を継承しており、UWidgetへの参照を追加することで、**SObjectWidget**が存在する間は**UWidget**がGCされないように保証します。
-   コントロールの破棄が開始されると、**SObjectWidget**の参照カウントが0になって破棄され、この時UWidgetへの参照が解除されます。
-   **UWidget**の参照が0になると、次回のGC時に解放され、この時初めて保持している**SWidget**の共有ポインタが解放されます。